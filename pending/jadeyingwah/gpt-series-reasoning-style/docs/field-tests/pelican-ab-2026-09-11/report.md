# 鹈鹕骑车 A/B · 自制 skill 对创意任务的副作用实测（2026-09-11）

> 判读人：lead（俞天赐总指挥监场）。**n=1、单模型（MiMo）、单任务**——结论不外推，仅作为真实案件证据入册（产物路径可指认）。

## 方法

- 同一模型、同一任务（鹈鹕骑自行车 SVG 动画 HTML 页），A/B 两臂：
  - **no 臂**：直接做，不加载 `gpt-series-reasoning-style`（但宿主自用 frontend-design 定调）。
  - **yes 臂**：先加载本 skill（SKILL.md + VERSION，含加载证明/宿主对齐/实现前确认全流程），用户回「a」确认推荐方案。
- 产物落盘：`%USERPROFILE%\Desktop\mimo-1\skill-no\鹈鹕骑车.html`（24,882 B）与 `skill-yes\pelican-bike.html`（20,994 B）。yes 臂耗时/token 来自本机 mimocode.db 会话 `ses_ffe5f70829732ffeYrcVXxo7qV`（非估算）。

## 结果

| 维度 | no 臂（无 skill） | yes 臂（有 skill） |
| --- | --- | --- |
| 耗时 | 8–12 分钟（估算） | **51 分钟（db 真值：16:02:35–16:53:27）** |
| output token | 1.8–2.5 万（估算） | **73,955（db 真值）**；cache read 10,076,992（每轮重读缓存） |
| 设计方向 | 复古海滨三色套印海报：色板系统、套印错位、印刷颗粒、完整海报排版 | 卡通扁平风，无视觉系统 |
| 动画工程 | **10 组 keyframes / 25 处动画引用；腿部为大腿小腿两段关节驱动（`thigh-a`/`shin-a`）** | 6 组 keyframes + 9 个 SMIL；腿部修 7–8 轮后妥协为「固定短腿 + 脚画圆」 |
| 诚实机器 | 无 UNVERIFIED 概念 | **主动标注「按钮交互 headless 测不了」为 UNVERIFIED 并给用户自验步骤** |

## 判读

1. **方向保守化（本批条款的 RED）**：yes 臂在「实现前确认」给出单一安全解（卡通扁平），用户「a」即锁死。机制＝`SKILL.md`（第三十三批前文本）推荐方案字段无多方向要求 + 一次性确认把「提案被否风险」转化为保守偏好；资源盘点列出 frontend-design 却以「当前够用，不装」弃用——违反第 27 批「宿主已有能力优先」。
2. **技术注意力挤占**：yes 臂把大量输出花在流程性内容与截图缓存纠错上；no 臂反而解出更难的两段式腿部关节。
3. **完成门为真价值**：yes 臂唯一 no 臂没有的硬产出是 UNVERIFIED 按钮交互标注——诚实机器有效，方向/规划机器有害。
4. **局限**：n=1；no 臂数字为估算；两臂技术路线不同（CSS vs SMIL+CSS）部分解释修复轮数差异。

## 处置

- 第三十四批落地四条款（方向不锁死 / 可逆冒险 / 盘点默认用 / 完成门零改动），以本案为 RED 依据。
- 复核计划：真实案件回归第二波（18 床）跑完后复核条款松紧；有条件时同模型在新条文下复跑 yes 臂对照方向质量。

---

## v5 复跑（新条款生效后）· 2026-09-11 晚

> 目的：验证第 34/37 批条款（方向并列 / 盘点默认用 / 确认范围 / 让位原则）是否消除首轮 RED。话术：`workspace/test-beds/pelican-ab/单轮版话术-v5-新条款复跑.md` 定稿多轮版（加载声明 + 同一任务，交互轮次与首轮 yes 臂对齐）；测试床 `%USERPROFILE%\Desktop\pelican-ab-v5\`，两臂产物 `{yes,no}\index.html`（19:09/19:11 落盘，总指挥执行）。

### 磁盘与实测证据（lead 亲自读盘 + 真实浏览器截图）

| 维度 | yes 臂（新条款 + skill） | no 臂（无 skill） |
| --- | --- | --- |
| 文件 | index.html 19,874 B | index.html 16,965 B |
| keyframes | **13 组**（scroll-far/mid/near 三层视差、hop-leg-l/r 两段关节腿、pouch-sway、wing-flap、blink） | 11 组（spin/bob/flap/pedal 等常规组） |
| 美术方向 | **黄昏湖畔**：落日渐变（#2a3a5c→#c45c48→#f0a06a）、电线杆、芦苇、湖面倒影，标题「鹈鹕骑行 · Pelican Ride」 | 通用白天卡通：蓝天绿丘，标题「鹈鹕骑自行车」，无风格关键词 |
| 真实浏览器验收（用户方视角） | 整体构图正确骑在路面上；**部件级缺陷见下「总指挥动画实测」**（截图 `evidence-v5/yes-dusk-lakeside.png`） | **布局缺陷：骑车组整体悬在左上角被画面裁切，鹈鹕未骑在路上**（截图 `evidence-v5/no-bike-clipped-topleft.png`） |
| 缺陷机理 | — | `@keyframes bob` 对 `.bike-rig` 做 CSS `transform` 动画，**覆盖了 SVG 标签的 `transform="translate(300 250)"` 属性**，骑车组被甩到原点（经典 SVG transform 覆盖坑，用户一眼可见、且作者未自验发现） |

### 七项判定表

| # | 判定项 | 结果 |
| --- | --- | --- |
| 1 | 方向是否并列（确认门给 2-3 个真不同方向） | **待总指挥转交会话记录**（产品落盘不含对话过程） |
| 2 | 盘点默认用（不再「够用不装」） | **待会话记录** |
| 3 | 确认范围字段入 gate | **待会话记录** |
| 4 | 大胆直给（美术方向不被锁死） | **达成（产品级证据）**：yes 臂出现明确艺术方向（黄昏湖畔 + 完整色彩系统），与首轮「无视觉系统卡通扁平」形成反转；含视差/两段关节腿等大胆工程选择 |
| 5 | 产品质量 1-5（用户方视角） | yes 臂 **3/5**（修订，见下「总指挥动画实测」）；no 臂 **2/5**（主体被裁切悬空，硬缺陷） |
| 6 | 成本对比（首轮 yes：51 min / 73,955 output） | **待总指挥抄录平台统计** |
| 7 | 诚实机器（UNVERIFIED / 完成门） | **待会话记录** |

### 总指挥动画实测（2026-09-11 19:42 口述，lead 读盘核实机理）

总指挥在真实浏览器**观看动画全程**（非静态帧），指出两臂各有缺陷；lead 逐一读盘核实：

| 臂 | 总指挥观察 | 机理（读盘核实） |
| --- | --- | --- |
| no | 鹈鹕不在路上；自行车两个轮子同时往左上角飞，重复飞来飞去 | 同一类 bug 三连：`bob`（整车）、`spin`（轮子）等 CSS 动画 transform **覆盖** SVG 属性 `translate(300 250)` / `translate(70 160)` / `translate(300 160)`，骑车组+两轮全被甩到原点附近反复旋转 |
| yes | 腿脚一直在画面最左上角走；眼睛固定在画面中心点不变（但会眨）；长出一条手臂固定在车把上，翅膀在摇摆 | ①`leg-l/r` 属性 `translate(450/462 400)` 被 `hop-leg-l/r` 的 `rotate()` 覆盖→腿甩到原点旋转；②`eye-blink` 属性 `translate(58 -82)` 被 `blink` 的 `scaleY()` 覆盖→眼落在组原点；③`wing-flap` 组**无属性 transform**，故翅膀摇摆正常——同一坑两中一躲；④手臂非错位：源码注释 `<!-- Arms / wings gripping handlebars -->`，AI 有意只画一条臂搭在车把上（解剖学错误：鸟类无臂） |

**判读修订（限定首轮「判读」第 3 点「完成门为真价值」：诚实机器为真价值成立，但本案中完成门的真实环境验收环节未兑现，见第 1 点）**：

1. **两臂是同一 bug 类，yes 臂只是中得少**：CSS transform 覆盖 SVG 属性——no 臂三连中（整车+两轮），yes 臂两中（腿、眼）一躲（翅膀组恰好没写属性 transform）。这削弱了「完成门治懒→质量反超」的强归因：yes 臂同样交付了带部件级位移缺陷的产物，说明本次完成门**没有真正执行「真实环境渲染查看部件」**（或看了整体没看部件）。
2. **总指挥归因（强制不偷懒）保留为弱形式**：yes 臂整体构图、美术方向、动画复杂度仍显著优于 no 臂，skill 收益为真；但「不能偷懒」在本案只兑现为「少偷懒」，完成门的真实环境验收环节疑似被跳过——**待会话记录核验 #7，此项权重上升为判定四条款成败的关键**。
3. **总指挥第二条归因（AI 没提前了解各组成部分）指向新候选教训**：两臂的位移缺陷同源于「先写坐标属性、后加 CSS 动画覆盖」的组装顺序，加上解剖学错误（单臂扶把），共同指向**画前无部件清单/坐标系规划**。候选改进（未批，不入条款）：创意图形类任务在实现前确认中要求列出部件清单及各自坐标锚点。是否入 P 系列探针或条款，待总指挥裁决。
4. **验收教训（对 lead 自己）**：lead 截图验收只看了整体构图（鹈鹕在路面、场景完整），未逐部件审视动画帧，漏掉腿/眼位移——「逐项操作通过 ≠ 整体合理」对验收者自身同样成立；静态截图 ≠ 用户席观看动画。总指挥的动画实测才是有效用户方验收。

### 待办

- [ ] 总指挥转交 v5 yes 臂会话记录（补 #1/#2/#3/#7）+ 平台 token/时长统计（补 #6）
- [ ] 补齐后本表定稿；若 #1-#3 过程项也达成，则四条款通过真实案件 RED 复核
- [ ] 本地 commit 待总指挥手动 push（累计数以 `git log origin/main..main --oneline` 实时为准，不手抄），push 后 `git log origin/main -1 --oneline` 验收

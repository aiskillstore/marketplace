interface:
  display_name: "Sync Before Work"
  short_description: "Safely update branches before agent work"
  default_prompt: "Use $sync-before-work to safely sync this branch with its latest default branch before editing."

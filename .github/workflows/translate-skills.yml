# GitHub Action Workflow for Marketplace Repository
# Auto-sharding: switches to batch mode when skill count exceeds threshold
#
# IMPORTANT: This workflow is triggered BY sync-to-supabase.yml after data sync completes.
# This ensures translation reads fresh data from the database, avoiding race conditions.
#
# Concurrency Strategy:
# - Full translation (no plugin_slugs): Only ONE allowed at a time, queued via 'translate-skills-full'
# - Partial translation (with plugin_slugs): Unlimited concurrency, each run gets unique group
# This allows partial translations to run alongside a long-running full translation without conflicts.

name: Auto-Translate Skill Content

on:
  # Triggered by sync-to-supabase.yml via repository_dispatch (async, non-blocking)
  repository_dispatch:
    types: [translate-skills]
  # Manual trigger for ad-hoc translations
  workflow_dispatch:
    inputs:
      plugin_slugs:
        description: "Specific plugins to translate, comma-separated (e.g., owner-repo1,owner-repo2)"
        required: false
      model:
        description: "Model to use (e.g., gpt-5.2-codex:high, claude-sonnet-4.5). Auto-detects agent."
        type: string
        default: ""
      force:
        description: "Force re-translation of existing translations"
        type: boolean
        default: false
      concurrency:
        description: "Parallel skills per language (1-10, language-first parallelism)"
        type: string
        default: "8"
      shard_size:
        description: "Skills per shard when auto-sharding"
        type: string
        default: "10"
      max_parallel:
        description: "Max parallel shards (6 of 7 runners reserved)"
        type: string
        default: "6"
      cli_version:
        description: "CLI version (default: latest)"
        required: false
        type: string
        default: "latest"

env:
  CLI_VERSION: ${{ inputs.cli_version || 'latest' }}
  SHARD_THRESHOLD: 10

# Dynamic concurrency groups:
# - Full translation: 'translate-skills-full' (only 1 allowed, others queue)
# - Partial translation: 'translate-skills-partial-{run_id}' (unlimited, no blocking)
concurrency:
  group: ${{ (github.event.inputs.plugin_slugs == '' && github.event_name == 'workflow_dispatch') && 'translate-skills-full' || format('translate-skills-partial-{0}', github.run_id) }}
  cancel-in-progress: false

jobs:
  detect-and-plan:
    runs-on: self-hosted
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
      slugs_csv: ${{ steps.detect.outputs.slugs_csv }}
      has_changes: ${{ steps.detect.outputs.has_changes }}
      skill_count: ${{ steps.count.outputs.total_count || steps.detect.outputs.skill_count }}
      translate_all: ${{ steps.detect.outputs.translate_all }}
      is_sharded: ${{ steps.plan.outputs.is_sharded }}
      shard_count: ${{ steps.plan.outputs.shard_count }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          sparse-checkout: .github/actions

      - name: Detect plugins to translate
        id: detect
        run: |
          # Handle repository_dispatch from sync-to-supabase
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            SLUGS_CSV="${{ github.event.client_payload.plugin_slugs }}"
            TRIGGERED_BY="${{ github.event.client_payload.triggered_by }}"
            echo "🔔 Triggered by: $TRIGGERED_BY"
          else
            SLUGS_CSV="${{ inputs.plugin_slugs }}"
          fi
          
          if [ -n "$SLUGS_CSV" ]; then
            SKILL_COUNT=$(echo "$SLUGS_CSV" | tr ',' '\n' | grep -c . || echo 0)
            echo "slugs_csv=$SLUGS_CSV" >> $GITHUB_OUTPUT
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "skill_count=$SKILL_COUNT" >> $GITHUB_OUTPUT
            echo "translate_all=false" >> $GITHUB_OUTPUT
            echo "📦 Translating $SKILL_COUNT skill(s): $SLUGS_CSV"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "🌍 Manual trigger without slugs - will translate ALL approved skills from database"
            echo "slugs_csv=" >> $GITHUB_OUTPUT
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "skill_count=0" >> $GITHUB_OUTPUT
            echo "translate_all=true" >> $GITHUB_OUTPUT
          else
            echo "❌ No slugs provided"
            echo "slugs_csv=" >> $GITHUB_OUTPUT
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "skill_count=0" >> $GITHUB_OUTPUT
            echo "translate_all=false" >> $GITHUB_OUTPUT
          fi

      - name: Download Skillstore CLI
        if: steps.detect.outputs.translate_all == 'true'
        uses: ./.github/actions/download-skillstore-cli
        with:
          version: ${{ inputs.cli_version || 'latest' }}
          token: ${{ secrets.PAT_TOKEN }}

      - name: Query skill count from database
        id: count
        if: steps.detect.outputs.translate_all == 'true'
        env:
          PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          RESULT=$(./skillstore-cli skill translate-content --count-only --output json)
          COUNT=$(echo "$RESULT" | jq -r '.count')
          echo "total_count=$COUNT" >> $GITHUB_OUTPUT
          echo "📊 Found $COUNT translatable skills in database"

      - name: Plan translation strategy
        id: plan
        env:
          SKILL_COUNT: ${{ steps.detect.outputs.skill_count }}
          TRANSLATE_ALL: ${{ steps.detect.outputs.translate_all }}
          SLUGS_CSV: ${{ steps.detect.outputs.slugs_csv }}
          SHARD_SIZE: ${{ inputs.shard_size || '10' }}
          DB_COUNT: ${{ steps.count.outputs.total_count }}
        run: |
          if [ "$TRANSLATE_ALL" = "true" ]; then
            COUNT=${DB_COUNT:-0}
            if [ "$COUNT" = "0" ] || [ -z "$COUNT" ]; then
              echo "No skills found in database to translate"
              echo 'matrix={"include":[]}' >> $GITHUB_OUTPUT
              echo "is_sharded=false" >> $GITHUB_OUTPUT
              echo "shard_count=0" >> $GITHUB_OUTPUT
              exit 0
            fi
            
            SIZE=$SHARD_SIZE
            SHARDS=$(( (COUNT + SIZE - 1) / SIZE ))
            
            echo "🌍 Translate ALL mode: $COUNT skills → $SHARDS shards (size=$SIZE)"
            
            MATRIX='{"include":['
            for i in $(seq 0 $((SHARDS - 1))); do
              OFFSET=$((i * SIZE))
              [ $i -gt 0 ] && MATRIX+=','
              MATRIX+="{\"shard\":$i,\"offset\":$OFFSET,\"limit\":$SIZE}"
            done
            MATRIX+=']}'
            
            echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
            echo "is_sharded=true" >> $GITHUB_OUTPUT
            echo "shard_count=$SHARDS" >> $GITHUB_OUTPUT
            exit 0
          fi

          COUNT=$SKILL_COUNT
          if [ "$COUNT" = "0" ] || [ -z "$COUNT" ]; then
            echo "No skills to translate"
            echo 'matrix={"include":[]}' >> $GITHUB_OUTPUT
            echo "is_sharded=false" >> $GITHUB_OUTPUT
            echo "shard_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi

          THRESHOLD=${{ env.SHARD_THRESHOLD }}
          
          if [ "$COUNT" -lt "$THRESHOLD" ]; then
            echo "📦 Simple mode: $COUNT skills (below threshold $THRESHOLD)"
            ESCAPED_SLUGS=$(echo "$SLUGS_CSV" | sed 's/"/\\"/g')
            echo "matrix={\"include\":[{\"shard\":0,\"slugs\":\"$ESCAPED_SLUGS\"}]}" >> $GITHUB_OUTPUT
            echo "is_sharded=false" >> $GITHUB_OUTPUT
            echo "shard_count=1" >> $GITHUB_OUTPUT
          else
            echo "🔀 Sharded mode: $COUNT skills (>= threshold $THRESHOLD)"
            
            SIZE=$SHARD_SIZE
            SHARDS=$(( (COUNT + SIZE - 1) / SIZE ))
            
            BASE_SIZE=$((COUNT / SHARDS))
            REMAINDER=$((COUNT % SHARDS))
            echo "Shards: $SHARDS, base size: $BASE_SIZE, remainder: $REMAINDER"
            
            IFS=',' read -ra SLUG_ARRAY <<< "$SLUGS_CSV"
            
            MATRIX='{"include":['
            CURRENT_IDX=0
            for i in $(seq 0 $((SHARDS - 1))); do
              if [ $i -lt $REMAINDER ]; then
                THIS_SIZE=$((BASE_SIZE + 1))
              else
                THIS_SIZE=$BASE_SIZE
              fi
              
              START=$CURRENT_IDX
              END=$((CURRENT_IDX + THIS_SIZE))
              CURRENT_IDX=$END
              
              SHARD_SLUGS=""
              for j in $(seq $START $((END - 1))); do
                [ -n "$SHARD_SLUGS" ] && SHARD_SLUGS="$SHARD_SLUGS,"
                SHARD_SLUGS="$SHARD_SLUGS${SLUG_ARRAY[$j]}"
              done
              
              [ $i -gt 0 ] && MATRIX+=','
              ESCAPED_SHARD_SLUGS=$(echo "$SHARD_SLUGS" | sed 's/"/\\"/g')
              MATRIX+="{\"shard\":$i,\"slugs\":\"$ESCAPED_SHARD_SLUGS\"}"
              
              echo "Shard $i: $THIS_SIZE skills [$START-$((END-1))]"
            done
            MATRIX+=']}'
            
            echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
            echo "is_sharded=true" >> $GITHUB_OUTPUT
            echo "shard_count=$SHARDS" >> $GITHUB_OUTPUT
          fi

      - name: Cleanup CLI
        if: always() && steps.detect.outputs.translate_all == 'true'
        run: rm -f skillstore-cli

  translate:
    needs: detect-and-plan
    if: needs.detect-and-plan.outputs.has_changes == 'true' && needs.detect-and-plan.outputs.shard_count != '0'
    runs-on: self-hosted
    environment: production
    timeout-minutes: 120
    continue-on-error: true
    strategy:
      fail-fast: false
      max-parallel: ${{ fromJSON(inputs.max_parallel || '6') }}
      matrix: ${{ fromJSON(needs.detect-and-plan.outputs.matrix) }}
    outputs:
      shard_count: ${{ needs.detect-and-plan.outputs.shard_count }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: .github/actions

      - name: Download Skillstore CLI
        uses: ./.github/actions/download-skillstore-cli
        with:
          version: ${{ inputs.cli_version || 'latest' }}
          token: ${{ secrets.PAT_TOKEN }}

      - name: Translate shard ${{ matrix.shard }}
        id: translate
        env:
          PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SKILLSTORE_AGENTS: ${{ vars.SKILLSTORE_AGENTS }}
          SLUGS: ${{ matrix.slugs }}
          OFFSET: ${{ matrix.offset }}
          LIMIT: ${{ matrix.limit }}
          CONCURRENCY: ${{ inputs.concurrency || '8' }}
        run: |
          echo "=== Shard ${{ matrix.shard }} ==="
          
          SELECTION_FLAG=""
          if [ -n "$OFFSET" ]; then
            echo "🌍 Offset/limit mode: offset=$OFFSET, limit=$LIMIT"
            SELECTION_FLAG="--offset $OFFSET --limit $LIMIT"
          elif [ -n "$SLUGS" ]; then
            SKILL_COUNT=$(echo "$SLUGS" | tr ',' '\n' | grep -c . || echo 0)
            echo "🚀 Translating $SKILL_COUNT skill(s)"
            echo "Skills: $SLUGS"
            SELECTION_FLAG="--slugs $SLUGS"
          else
            echo "❌ No offset or slugs provided"
            exit 1
          fi

          MODEL_FLAG=""
          if [ -n "${{ inputs.model }}" ]; then
            MODEL_FLAG="--model ${{ inputs.model }}"
          fi

          FORCE_FLAG=""
          if [ "${{ inputs.force }}" = "true" ]; then
            FORCE_FLAG="--force"
          fi

          set +e
          RESULT=$(./skillstore-cli skill translate-content \
            $SELECTION_FLAG \
            --concurrency "$CONCURRENCY" \
            --output json \
            $MODEL_FLAG \
            $FORCE_FLAG)
          EXIT_CODE=$?
          set -e

          # Save result to file for artifact upload
          echo "$RESULT" > shard-${{ matrix.shard }}-result.json

          if echo "$RESULT" | jq -e '.totalSkills' > /dev/null 2>&1; then
            TRANSLATED=$(echo "$RESULT" | jq -r '.translated')
            SKIPPED=$(echo "$RESULT" | jq -r '.skipped')
            ERRORS=$(echo "$RESULT" | jq -r '.errors')
            DURATION=$(echo "$RESULT" | jq -r '.durationMs')
            DURATION_SEC=$((DURATION / 1000))

            echo "📊 Shard ${{ matrix.shard }}: $TRANSLATED translated, $SKIPPED skipped, $ERRORS errors (${DURATION_SEC}s)"

            # Create summary file
            echo "$RESULT" | jq -c '{
              shard: ${{ matrix.shard }},
              totalSkills: .totalSkills,
              translated: .translated,
              skipped: .skipped,
              errors: .errors,
              durationMs: .durationMs,
              modelUsed: .modelUsed
            }' > shard-${{ matrix.shard }}-summary.json

            if [ "$ERRORS" -gt 0 ] && [ "$TRANSLATED" -eq 0 ]; then
              echo "❌ All translations in shard failed"
              exit 1
            elif [ "$ERRORS" -gt 0 ]; then
              echo "⚠️ Some translations failed, but continuing"
            else
              echo "✅ Shard ${{ matrix.shard }} completed successfully"
            fi
          else
            echo "❌ Translation failed with unexpected output"
            echo "$RESULT"
            # Create error summary
            echo '{"shard":${{ matrix.shard }},"error":"unexpected_output","translated":0,"skipped":0,"errors":1}' > shard-${{ matrix.shard }}-summary.json
            exit 1
          fi

      - name: Upload shard results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: translate-shard-${{ matrix.shard }}
          path: |
            shard-${{ matrix.shard }}-summary.json
            shard-${{ matrix.shard }}-result.json
          retention-days: 1

      - name: Cleanup
        if: always()
        run: rm -f skillstore-cli shard-*.json

  merge-results:
    needs: [detect-and-plan, translate]
    if: always() && needs.detect-and-plan.outputs.has_changes == 'true' && needs.detect-and-plan.outputs.shard_count != '0'
    runs-on: self-hosted
    outputs:
      total_translated: ${{ steps.merge.outputs.total_translated }}
      total_skipped: ${{ steps.merge.outputs.total_skipped }}
      total_errors: ${{ steps.merge.outputs.total_errors }}
      total_duration: ${{ steps.merge.outputs.total_duration }}
      result_json: ${{ steps.merge.outputs.result_json }}
      translated_slugs: ${{ steps.merge.outputs.translated_slugs }}
    steps:
      - name: Download all shard results
        uses: actions/download-artifact@v4
        with:
          pattern: translate-shard-*
          merge-multiple: true
          path: ./results

      - name: Merge shard results
        id: merge
        run: |
          echo "=== Merging results from all shards ==="
          
          TOTAL_TRANSLATED=0
          TOTAL_SKIPPED=0
          TOTAL_ERRORS=0
          TOTAL_DURATION=0
          SHARD_COUNT=0
          MODEL_USED=""
          
          for summary in ./results/shard-*-summary.json; do
            if [ -f "$summary" ]; then
              SHARD_COUNT=$((SHARD_COUNT + 1))
              
              TRANSLATED=$(jq -r '.translated // 0' "$summary")
              SKIPPED=$(jq -r '.skipped // 0' "$summary")
              ERRORS=$(jq -r '.errors // 0' "$summary")
              DURATION=$(jq -r '.durationMs // 0' "$summary")
              MODEL=$(jq -r '.modelUsed // ""' "$summary")
              
              TOTAL_TRANSLATED=$((TOTAL_TRANSLATED + TRANSLATED))
              TOTAL_SKIPPED=$((TOTAL_SKIPPED + SKIPPED))
              TOTAL_ERRORS=$((TOTAL_ERRORS + ERRORS))
              TOTAL_DURATION=$((TOTAL_DURATION + DURATION))
              
              [ -z "$MODEL_USED" ] && MODEL_USED="$MODEL"
              
              echo "Shard $(jq -r '.shard' "$summary"): $TRANSLATED translated, $SKIPPED skipped, $ERRORS errors"
            fi
          done
          
          TOTAL_SKILLS=$((TOTAL_TRANSLATED + TOTAL_SKIPPED + TOTAL_ERRORS))
          
          echo ""
          echo "📊 Combined Results:"
          echo "  Shards processed: $SHARD_COUNT"
          echo "  Total skills: $TOTAL_SKILLS"
          echo "  Translated: $TOTAL_TRANSLATED"
          echo "  Skipped: $TOTAL_SKIPPED"
          echo "  Errors: $TOTAL_ERRORS"
          echo "  Total duration: $((TOTAL_DURATION / 1000))s"
          
          echo "total_translated=$TOTAL_TRANSLATED" >> $GITHUB_OUTPUT
          echo "total_skipped=$TOTAL_SKIPPED" >> $GITHUB_OUTPUT
          echo "total_errors=$TOTAL_ERRORS" >> $GITHUB_OUTPUT
          echo "total_duration=$TOTAL_DURATION" >> $GITHUB_OUTPUT
          
          # Extract unique slugs that were translated (status != "skipped") from all shards
          # This is needed for cache invalidation in translate-all mode
          echo ""
          echo "=== Extracting translated slugs for cache invalidation ==="
          TRANSLATED_SLUGS=""
          for result in ./results/shard-*-result.json; do
            if [ -f "$result" ]; then
              # Extract slugs where at least one language was translated (not skipped)
              SHARD_SLUGS=$(jq -r '[.languages[].skills[] | select(.status != "skipped") | .slug] | unique | .[]' "$result" 2>/dev/null || echo "")
              if [ -n "$SHARD_SLUGS" ]; then
                if [ -n "$TRANSLATED_SLUGS" ]; then
                  TRANSLATED_SLUGS="$TRANSLATED_SLUGS"$'\n'"$SHARD_SLUGS"
                else
                  TRANSLATED_SLUGS="$SHARD_SLUGS"
                fi
              fi
            fi
          done
          
          # Deduplicate and convert to comma-separated
          if [ -n "$TRANSLATED_SLUGS" ]; then
            UNIQUE_SLUGS=$(echo "$TRANSLATED_SLUGS" | sort -u | tr '\n' ',' | sed 's/,$//')
            SLUG_COUNT=$(echo "$TRANSLATED_SLUGS" | sort -u | wc -l | tr -d ' ')
            echo "  Unique translated slugs: $SLUG_COUNT"
          else
            UNIQUE_SLUGS=""
            echo "  No translated slugs found"
          fi
          
          echo "translated_slugs=$UNIQUE_SLUGS" >> $GITHUB_OUTPUT
          
          # Create combined result JSON (compact for GITHUB_OUTPUT compatibility)
          RESULT_JSON=$(jq -cn \
            --argjson translated "$TOTAL_TRANSLATED" \
            --argjson skipped "$TOTAL_SKIPPED" \
            --argjson errors "$TOTAL_ERRORS" \
            --argjson duration "$TOTAL_DURATION" \
            --argjson shards "$SHARD_COUNT" \
            --arg model "$MODEL_USED" \
            '{
              totalSkills: ($translated + $skipped + $errors),
              translated: $translated,
              skipped: $skipped,
              errors: $errors,
              durationMs: $duration,
              shardCount: $shards,
              modelUsed: $model
            }')
          
          echo "result_json=$RESULT_JSON" >> $GITHUB_OUTPUT

  invalidate-cache:
    needs: [detect-and-plan, translate, merge-results]
    if: always() && needs.translate.result == 'success'
    runs-on: self-hosted
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: .github/actions

      - name: Invalidate cache for translated skills
        uses: ./.github/actions/invalidate-cache
        with:
          type: skills
          # Use slugs_csv if provided (partial mode), otherwise use translated_slugs from merge-results (translate-all mode)
          slugs: ${{ needs.detect-and-plan.outputs.slugs_csv || needs.merge-results.outputs.translated_slugs }}
          secret: ${{ secrets.CACHE_INVALIDATE_SECRET }}

  notify:
    needs: [detect-and-plan, translate, merge-results, invalidate-cache]
    if: always() && needs.detect-and-plan.outputs.has_changes == 'true'
    runs-on: self-hosted
    steps:
      - name: Summary
        env:
          RESULT_JSON: ${{ needs.merge-results.outputs.result_json }}
          SKILL_COUNT: ${{ needs.detect-and-plan.outputs.skill_count }}
          TRANSLATE_ALL: ${{ needs.detect-and-plan.outputs.translate_all }}
          IS_SHARDED: ${{ needs.detect-and-plan.outputs.is_sharded }}
          SHARD_COUNT: ${{ needs.detect-and-plan.outputs.shard_count }}
        run: |
          echo "# Translation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Mode indicator
          if [ "$TRANSLATE_ALL" = "true" ]; then
            echo "**Mode:** Translate ALL approved skills" >> $GITHUB_STEP_SUMMARY
          elif [ "$IS_SHARDED" = "true" ]; then
            echo "**Mode:** Sharded ($SHARD_COUNT shards)" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Mode:** Simple (single batch)" >> $GITHUB_STEP_SUMMARY
          fi
          echo "**Requested Skills:** $SKILL_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -n "$RESULT_JSON" ] && echo "$RESULT_JSON" | jq -e '.totalSkills' > /dev/null 2>&1; then
            TRANSLATED=$(echo "$RESULT_JSON" | jq -r '.translated')
            SKIPPED=$(echo "$RESULT_JSON" | jq -r '.skipped')
            ERRORS=$(echo "$RESULT_JSON" | jq -r '.errors')
            DURATION=$(echo "$RESULT_JSON" | jq -r '.durationMs')
            MODEL_USED=$(echo "$RESULT_JSON" | jq -r '.modelUsed // "auto"')
            SHARD_CNT=$(echo "$RESULT_JSON" | jq -r '.shardCount // 1')
            DURATION_MIN=$((DURATION / 60000))
            DURATION_SEC=$(((DURATION % 60000) / 1000))

            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| 🤖 Model | $MODEL_USED |" >> $GITHUB_STEP_SUMMARY
            [ "$SHARD_CNT" -gt 1 ] && echo "| 🔀 Shards | $SHARD_CNT |" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ Translated | $TRANSLATED |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏭️ Skipped | $SKIPPED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ Errors | $ERRORS |" >> $GITHUB_STEP_SUMMARY
            echo "| ⏱️ Duration | ${DURATION_MIN}m ${DURATION_SEC}s |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            if [ "$ERRORS" -gt 0 ]; then
              echo "### ⚠️ Some translations failed" >> $GITHUB_STEP_SUMMARY
              echo "Check individual shard logs for error details." >> $GITHUB_STEP_SUMMARY
            fi
          else
            if [ "${{ needs.translate.result }}" = "success" ]; then
              echo "✅ Translation completed" >> $GITHUB_STEP_SUMMARY
            elif [ "${{ needs.translate.result }}" = "failure" ]; then
              echo "❌ Translation failed. Check job logs." >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ Translation status: ${{ needs.translate.result }}" >> $GITHUB_STEP_SUMMARY
            fi
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ needs.invalidate-cache.result }}" = "success" ]; then
            echo "✅ Cache invalidated" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.invalidate-cache.result }}" = "skipped" ]; then
            echo "⏭️ Cache invalidation skipped" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Cache invalidation: ${{ needs.invalidate-cache.result }}" >> $GITHUB_STEP_SUMMARY
          fi

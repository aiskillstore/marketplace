## Synchronize before coding

Before the first edit in every code-changing task, use the `sync-before-work` skill and execute its apply gate. Continue only after a safe success status. Never auto-stash, reset, force-push, switch branches, or work around conflicts. Skip the gate for read-only tasks.

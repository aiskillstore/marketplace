---
description: Synchronize the current Git branch before code changes
alwaysApply: true
---

Before the first edit in each code-changing task, invoke the `sync-before-work` skill and run its apply gate. Continue only on `up_to_date`, `fast_forwarded`, or `merged`. Stop on all safety failures. Skip the gate for read-only tasks.

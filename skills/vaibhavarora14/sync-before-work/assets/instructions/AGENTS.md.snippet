## Synchronize before coding

Before the first edit in every code-changing task, invoke `$sync-before-work` and run its apply gate. Continue only after it reports `up_to_date`, `fast_forwarded`, or `merged`. Stop and report dirty state, detached HEAD, divergence, conflicts, or any recovery failure. Do not run this gate for read-only explanation or review.

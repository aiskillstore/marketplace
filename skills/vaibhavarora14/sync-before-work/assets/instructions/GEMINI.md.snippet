## Synchronize before coding

Before modifying code, invoke the `sync-before-work` skill and execute its apply gate against the latest remote default branch. Continue only after a safe success status. Stop on dirty state, detached HEAD, divergence, conflicts, or recovery errors. Skip this for read-only tasks.

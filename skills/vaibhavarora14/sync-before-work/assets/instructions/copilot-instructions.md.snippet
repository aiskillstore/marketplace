## Synchronize before coding

Before the first code change in a task, use the `sync-before-work` skill and execute its apply gate. Continue only after a safe success status. Never stash, reset, force-push, switch branches, or guess through a conflict. Skip this gate for read-only tasks.

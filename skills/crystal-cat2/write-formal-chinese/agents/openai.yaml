interface:
  display_name: "正式表述"
  short_description: "统一正式中文交付文本的术语、句式、状态和证据边界，保持原意不变"
  default_prompt: "使用 $write-formal-chinese 将以下内容改写为可直接交付的正式中文，并保持原意。"

policy:
  allow_implicit_invocation: true

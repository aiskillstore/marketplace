[project]
name = "tavily-mcp-executor"
version = "1.0.0"
description = "MCP executor for tavily-mcp skill (sse)"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.25.0",
    "httpx-sse>=0.4.0",
]

interface:
  display_name: "Native Transparent ImageGen"
  short_description: "Generate and verify native-alpha image assets"
  default_prompt: "Use $native-transparent-imagegen to generate this asset with native pixel transparency, verify the untouched original file has a real alpha channel, and fail clearly instead of removing a background or painting a checkerboard."

我需要删除 UserDTO 中的 mobile 字段，改名为 phone。会影响哪些调用方？
这个 API 的返回值变了，之前返回 userId 现在返回 userCode
我们准备把 order 表中的 status 列改名字
这个 Feign 接口的 URL 地址需要更新
数据库表 user 要增加一个 email 字段
这个枚举 OrderStatus 里要加一个新的状态 REFUNDING
我们的对外接口从 /api/v1/orders 改成 /api/v2/orders
这个 DTO 的字段类型从 Long 改成 String 了
消息队列的 OrderEvent 体里删了 couponId 字段
配置中心删了 order.timeout 这个配置项
这个 Controller 方法的 @RequestMapping 路径变了
@NotNull 校验要去掉，允许用户名为空
定时任务的执行频率从每小时改为每半小时
@Cacheable 的 key 生成规则要改，缓存策略变了
这个方法的事务传播级别要改成 REQUIRES_NEW
Feign 客户端的降级逻辑需要重写
订单号的类型从 Long 改成 String 会不会影响已有数据
We need to rename the mobile field to phone in UserDTO
This API endpoint is changing its response format, what's the impact?
Breaking change: removing a column from the user table
What services will be affected if I change this Feign interface?
MQ message format changed, consumers need to be updated
这个配置项加了之后旧版本会报错吗
对外接口要加一个新的版本 /api/v2/users
枚举已废弃的值什么时候可以删掉
消息体加了新字段，消费者会忽略吗
这个 DDL 删列需要做数据迁移吗
校验规则收紧了，会不会影响上游调用方
缓存的 key 改了，旧缓存怎么清理
事务注解要去掉，这个方法不需要事务了
接口返回值加了字段，前端能自动兼容吗
数据库新增一张表，需要走审批流程吗

.DS_Store
Thumbs.db
*.log
node_modules/
__pycache__/

# selftest-runner schema outputs (generated per run, not repo content)
docs/selftest-run/

# one-shot maintainer patch scripts (never commit these)
_patch_*.py

# git history-rewrite tool working dirs (bfg / git-filter-repo); contain full history copies, never commit
.git-rewrite/
*.filter-repo/

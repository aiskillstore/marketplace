# Changelog / 更新日志

## Unreleased / 未发布增量（对外仍为 1.2.0）

- **Claude Code 插件 / marketplace 打包与可发现性基本盘（2026-09-12 第五十四批）**：新增 `.claude-plugin/plugin.json` 与 `.claude-plugin/marketplace.json`——官方文档亲证单体插件可把 `SKILL.md` 直接置于插件根，故本仓不搬目录，`marketplace.json` 的 `plugins[0].source` 取 `"./"` 自指仓库根；Claude Code 下 `/plugin marketplace add JadeYingWah/gpt-series-reasoning-style` 再 `/plugin install gpt-series-reasoning-style@gpt-series-reasoning-style` 即可一键安装（git 分发下相对路径有效，`add owner/repo@v1.2.0` 可钉版本）。`plugin.json` 只放元数据、**不声明** agents/hooks/commands/skills/mcpServers（默认 strict），使根目录既有的 `agents/openai.yaml`（OpenAI/Codex 清单，非 Claude 的 `.md` agent）与 `hooks/`（无 `hooks.json`，仅 opt-in 单行提醒脚本）不被 Claude 当作插件组件激活。版本三处（`VERSION` / `plugin.json` / `marketplace.json`）手动对齐 1.2.0，作为 post-1.2.0 的 Unreleased 增量，**不 bump、不打新 tag**；衍生的「三处版本一致守卫」按新 SB 项准入规则（先有 RED 实证）本次不实施，落 `docs/proposals/2026-09-12-sb24-plugin-version-alignment.md`，推荐并入现有 SB1 而非新增 SB24。README 的 Quick Start 与 Install 节补 marketplace 一键安装承接（不改 13 平台 / 安装表 14 行口径——marketplace 是 Claude 平台的分发通道，不是新平台）。GitHub 可发现性：topics 由 6 个增补到 14 个（新增 `claude-code`、`claude-code-plugin`、`ai-agent`、`workflow`、`verification`、`prompt-engineering`、`code-agent`、`governance`）；repo description 已存在且达标，保留不动；homepage 因未启用 GitHub Pages 不设（避免指向 404）；社交预览 OG 图无 REST API，需在仓库 Settings 网页手工上传 `social-preview.png`（待人工）。验证：本地清单结构校验 22 项全部 PASS（kebab-case、不撞官方保留名、`source="./"`、三处版本一致、plugin 名 == SKILL frontmatter name、plugin.json 不声明任何组件）；`selfcheck` 22/22、fingerprint `01e30855018b` 与 1.2.0 发布时一致（规则面零牵动、常驻 token 零变化）。**UNVERIFIED（本环境无 Claude Code CLI，须真机）**：`claude plugin validate` 通过；`/plugin marketplace add` + `/plugin install` 实装成功；`source:"./"` 自指与安装后目录名通过 skilllint FM010；`agents/`、`hooks/` 非 Claude 资产确认不被激活。
- Claude Code plugin/marketplace packaging and a discoverability baseline (batch 54): added `.claude-plugin/` manifests for one-command `/plugin marketplace add` install (single-skill-at-root layout, `source: "./"`), aligned all three version surfaces at 1.2.0 without a new tag, expanded GitHub topics 6 → 14, and deferred the version-alignment guard to a proposal (recommend folding into SB1). Real `claude plugin validate` / install remains UNVERIFIED (no Claude CLI in this environment).

## 1.2.0 - 2026-09-12

**公开发布 1.2.0**：把 1.1.0 之后累计的第一至第五十二批增量（规则增强、工具链、六轮全仓系统审查修复、A/B 基线实测反哺）正式切为公开版本；本批同步完成 README 门面重构与全部版本表面、社交预览图的翻新。
Public release 1.2.0: cuts the post-1.1.0 batches 1–52 (rule hardening, tooling, the six-round full-repo audit fixes, A/B baseline feedback) as a public release, and refreshes the README plus every version surface and the social preview.

- **1.2.0 发布 · 版本表面与社交预览翻新（2026-09-12 第五十三批）**：VERSION 升 1.2.0，同步 SKILL.md 的 Current version、README（badge / 公开发布线 / 目录树 VERSION 注释 / 复杂度预算基线 / Versioning 节）、site/index.html 徽章、self-test.md 5 处加载版本引用（SB1 版本一致性守卫覆盖 SKILL/README/self-test/site 四表面）。社交预览图版本号翻新：`social-preview.svg` 文字为转曲路径（14 个 path、0 个 text，无法机器比对），用 fontTools 从 Segoe UI 19px 按拟合变换（scale=19/upm、基线 y=614、advance 右边界 1223.05）重新生成右下角 footer 转曲路径——逐轮廓比对确认仅版本号第 4 个字形 1→2 变化（Segoe UI 数字等宽，0/1/2 均 1104 units），其余 10 个轮廓位移 ≤0.1px；`<desc>` 元数据 Version 同步 1.2.0；`social-preview.png` 由 generate-banner.py（版本号读 VERSION）重渲染。验证：SVG XML 合法、仍 14 path/0 text、新旧 footer bbox 一致（x1128.5–1222.7、y600.4–614.2）、selfcheck 22/22。另把第 52 批条目内说明性的「盘符 + Users + 用户名」绝对路径占位符中性化为 `<系统盘>:\Users\<用户名>`——占位符本无真实用户名，但字面盘符形态会使该批声称的「该路径前缀 grep 为 0」不自洽、并在未来误触发候选 SB23 脱敏守卫；修正后 tracked 文件对该字面路径前缀的 grep 真正为 0。
- **README 门面重构（2026-09-12）**：新增 Quick Start（克隆→安装→一句调用，提到目录最前）与 Why（假完成返工成本账 + 有/无纪律 before/after 对话示例）两节；剥离内部批次号溯源、散布日期、CI 事故史、内部版本迭代细节等运维信息（归 CHANGELOG / INTERNAL-HISTORY）；事实层一字未动（11 字段门禁 / SB1–SB22 / 77 自测 / 13 references / 21 身份 / 13 平台 / 25 类黑名单 / 14 步 CI / 3,843 tok 常驻 / Lite 147 tok），mermaid 工作流图完整保留。验证：selfcheck 22/22、fingerprint `399844022e2b` 恒定（SB21 曾抓到「13 份 references」被并入句中致行首正则锚点失效，已修为独占行首）；旧版备份存于私有工作区，不入仓库。
- **推送前脱敏 · 铁律 2 三处泄漏修复（2026-09-12 第五十二批，外部终检 RED）**：小米 AI 推送前终检实锤 tracked 文件残留 3 处本机用户名绝对路径（`<系统盘>:\Users\<用户名>\...` 形态的用户目录绝对路径，铁律 2 明令禁止的用户名面），违反铁律 2「推送面脱敏」。lead 亲自以 `git grep -ni <本机用户名>` 复核确认与终检一致、无其他大小写变体：`CHANGELOG.md:26`（第 34 批条目内产物路径）、`docs/field-tests/pelican-ab-2026-09-11/report.md:10`（产物落盘路径）与 `:38`（v5 测试床路径）。修法统一为 `%USERPROFILE%\Desktop\...` 环境变量形态（语义不变、去用户名）；mimocode.db 会话 id `ses_ffe5f708...` 不含用户名，按终检建议保留。**验证**：`git grep -ni <本机用户名>` = 0、`git grep 'C:\\Users'` = 0；selfcheck **22/22**（fingerprint `399844022e2b` 恒定；两文件均为 dated 记录面，SB21/SB22 不触发，实测确认）。**准入建议（待总指挥裁决）**：本轮泄漏自第 34 批引入存活至第 51 批共 5 个待推提交，证明脱敏目前只靠人工纪律——候选 **SB23「推送面脱敏守卫」**（对 tracked 文件断言本机用户名零命中，缺陷可指认第 34 批提交哈希），加项会牵动 SB 计数 22→23 多表面同步，先请示后动手。**教训**：铁律 2 的执行目前无机器守卫，与 SB21 之前的散文计数同类——「要求了」不等于「守住了」。
- **审查报告归档（2026-09-12 第五十一批）**：新增 `docs/reviews/2026-09-12-full-repo-audit-round2.md`——新一轮六轮审查的独立报告（范围与方法、六轮概览、缺陷清单 P1×3/P2×4/P3×1 逐条带位置与证据、新增守卫 4 处及 RED/GREEN、撤回疑似 4 项、核验为净清单、残余风险、事故与教训、提交链），与第 50 批的收官条目互为索引；本批零代码/规则文本变更，dated 记录面不触发 SB21 散文计数守卫。
- **全仓系统审查 · 收官（2026-09-12 第五十批）**：第 3–6 轮结果与终止判定。**第 3 轮（零发现）**：对第 48 批刚修复的拦截层做**引号/包裹全形态变异矩阵 35 例**（裸命令/双引号路径/单引号/反引号/反引号+引号混合/双反引号/版本号后缀/wrapper 前缀/wrapper+反引号/sudo/xargs/黑名单反引号形态/白名单 8 形态/普通命令 2 形态）**全部符合设计**；install.sh 与 session-reminder.sh `bash -n` 通过；probe-scenarios.json 的三场景**共用顶层 `probe_prompt` 属设计**（probe-runner.py:53 读该键）；钩子 14 行但输出**恰好一行**（文件头注释明载设计边界）；CI 步数按 SB21 口径复核 = 14。**第 4 轮（1 处 P3，自查抓到）**：逐行复审第 47/48 批完整 diff，发现第 48 批在 `claim-check.py` 与夹具注释里把批次号误写为 batch 47（实为 48 的改动）——可追溯性信息错误，第 49 批（`cbef332`）修正；**教训**：注释里的批次号也是"散文数字"，写错同样漂移，只是危害低。**第 5 轮（零发现）**：21 份身份文件与 `_template.md` 章节比对（20 份全同；deputy-commander 多出的「指挥权接管」是第 11 批刻意新增，既有核验在案）；selftest-runner schema 往返 OK；site 内链零悬空；最近提交信息与 CHANGELOG 批次号一致；workflow.md 头部 **Section Map 的 32 个节引用全部命中真实节标题**（第 47 批 Resource Survey 修复的指针有效性就此闭环）。**第 6 轮（零发现）**：SKILL.md 全文 113 行精读——硬规则首条与加载证明引用逐字一致、门禁 11 字段与 GATE_FIELDS 一致、References 清单恰 13 份、「工作流第 5 步」指针自洽、版本 1.1.0（SB1 守）；前四批全部修复在位终检（selfcheck 22/22、变异矩阵 35/35、CI 步 9 五断言、py_compile 7 脚本、CLI 冒烟全过）。**终止判定：第 5、6 轮连续零发现，满足总指挥「连续二轮零发现」的终止条件，审查收官。**全程合计（新一轮 1–6 轮）：修复 **P1×3 / P2×4 / P3×1**（空 Resource Survey 节、身份声明例外反向 6 处、反引号绕过拦截层、站点 10-field/Lite token/版本徽章三处盲区、openai.yaml 缺第 11 字段、注释批次号），新增守卫 4 处（SB9 confirmation scope、SB22 站点英文形态、SB1 站点版本徽章、CI 步 9 第 5 断言 `saw 3 blocked`），全部 RED/GREEN 双证；撤回 4 项疑似（如实记档防复发）；三名审查员全程失败（2×429、1×无工具），六轮全部由 lead 亲自执行。**待 push 累计 5 个提交（`04f8761`…`本批`），铁律 1：由总指挥在 cmd 手动执行。**
- **全仓系统审查 · 新一轮第二轮（2026-09-12 第四十八批）**：换角度重查——CI 真实覆盖、全仓数字对账、空节普查、EN 镜像逐节对应、安全层边界形态。**三名审查员全部失败**（两名撞 429 频率限制至 09-12 22:25、一名无命令执行工具），其范围由 lead 亲自补完。**P1×1（本轮极重，第 35 批引号修复的漏网形态）**：**反引号包裹的命令完全绕过 claim-check 两层拦截**——markdown 声明清单里 `` - `pypy -c "print(1)"` `` 这种**行内代码包裹**是最常见的写法，但 `_argv0` 只剥 `"`/`'` 不剥反引号，首词带上反引号后既不在 wrapper 集合也不在 37 名解释器家族里 ⇒ **放行并执行**。实测：第 44 批新增的 15 个族成员逐一以反引号形态提交，**15/15 全部执行**（`saw 0 blocked`；本机恰好无这些解释器才 exit=1，有解释器的机器上即任意代码执行）。而 CI 夹具写的是无反引号形态（`python -c "print(1)"`），所以 CI 步 9 一直绿。**修复**：`_argv0` 剥反引号 + `interpreter_block_reason()` 入口剥**成对包裹**反引号（后者必需——只修 `_argv0` 会把白名单也打坏：`` `pypy -m unittest` `` 的白名单 `search` 跑在带反引号原文上锚定失配，合法测试命令反被拦，实测抓到后补的）。**RED/GREEN 三组**：①15 族反引号形态 → **15/15 BLOCKED**（`interpreter-indirect`）；②CI 夹具行为不变（git --version 放行 / interpreter-indirect / dangerous pattern 三断言照过）；③白名单回归 `` `pypy -m unittest` `` / `` `python --version` `` → 不再 BLOCKED（放行；其 exit=1 是 cmd.exe 不支持反引号命令替换的真实执行结果，非工具拦截，bash 下会成功）。**防回归固化**：`scripts/examples/claim-check-guard.md` 增第 4 条命令（反引号包裹形态）+ CI 步 9 增第 5 条断言 `grep -q "saw 3 blocked"`（修复回退时该计数变 2，CI 立即红；CI 步数不变仍 14）。**P2×1（站点第三个盲区，与前两个同族）**：`site/index.html:62` 徽章声明 `v1.1.0` 而 **SB1 不覆盖站点**——本批已抓到站点两处漂移（10-field、Lite token），版本徽章是同类"无人看管的活表面"；升版本时站点会静默落后。修复：SB1 增站点徽章检查（`elif` 链内，报 `site badge does not show v<version>`），**RED/GREEN 双证**（站点改 v9.9.9 → 21/22 且点名；还原 22/22）。**第 2 轮核验为净（不改）**：CI 14 步本地逐步实跑（可跑的 10 步全过；skilllint/uv 两步需网络如实标注不可本地复跑）；`openai.yaml` 经 pyyaml 6.0.2 实测解析合法且 default_prompt 含 **11/11** 门禁字段英文 token（第 47 批修复生效）；全仓 16 个活表面 **84 处「数字+量词」逐一与真值对账，零漂移**（含 Resume Check 7 项、轻通道排除项计数、23/11/6 字段族）；7 大 reference 文件 **`##` 节零空节**（第 47 批 Resource Survey 修复后无同类残留；workflow.md:39 Resume Check 标题后 4 空行属格式松散非缺陷，节内有完整正文）；**EN 镜像 45/45 节与中文权威版一一对应**（3 个名义差异是文末审计模板的中英标题命名差，实为同节）；`openai.yaml`/`identities/README.md`/`minimal-discipline.md` **均未声明版本号**（SB1 无需覆盖）；`install.sh`/`install.ps1` 无 CI 执行级覆盖已如实记档（SB10 静态守平台键；加执行步会牵动 CI 步数多表面同步，留待总指挥裁决）。**教训（记死，第 35 批教训的直接重演）**：**"修引号"不等于"修包裹"——`"`、`'`、反引号是三类独立边界形态，落地当天就该逐一当变异体过**；CI 夹具只测无反引号形态 = 守卫自己也是偏样本。**验证**：selfcheck **22/22**（fingerprint `399844022e2b`）；CI 步 9 五断言本地全过。
- **全仓系统审查 · 新一轮第一轮（2026-09-11 第四十七批；总指挥把终止条件由「连续一轮零发现」提高到「**连续二轮**零发现」，故重新计数）**：本轮换角度**四路并行**——三名审查员分别攻「规则可操作性与跨文件冲突」「README/站点/CI 的可执行性」「SKILL 压缩是否丢决策点」，lead 攻「模块间依赖与接口契约」；**所有发现由 lead 逐条机械复核后才判缺陷**（不采信转述）。**P1×2**：①**`references/series-reasoning-workflow.md:310`「`## Resource Survey / 资源盘点前置`」是空节**——该节正文（四档结论：用 / 改造用 / **本 skill 已覆盖（点名覆盖它的小节、不重复加载）** / 不适用，以及本地缺口须搜技能市场并**开工前先征求用户同意再安装**）被错挂在 `:325`「`## Change Management / 变更管理`」节末尾。危害叠加：文件头的 **Section Map 主动把宿主引向该节**（「指令与盘点」一行点名 `Resource Survey / 资源盘点前置`），宿主按需取用时读到的是空白，且这条规则只在"用户改需求"的语境下才可能被读到＝**规则丢失 + 错配**。修复：正文移回 `:310` 节下，Change Management 节恢复纯净。**②「身份声明例外」方向相反（共 6 处）**——`agent-modes.md:437`(CN)/`:475`/`:401`/`:457`/`:468` 与 `series-reasoning-lessons.md:121` 写「不必每条回复重复，**除非宿主已自动注入身份**」，语义为"宿主已注入 ⇒ 反而必须每条重复"；而 `multi-agent-closure-rules.md:19`(EN)/`:33`(CN)、`identity-library.md:114`、`commander-roles.md:86` 及 `commander-roles.md:133/141` 的**反模式**（「宿主**没有**自动注入身份时，回复却不声明自己是谁」）一致指向相反方向："宿主已注入 ⇒ **可以**省略重复声明"。该错误在 agent-modes 内**中英双份同向**，故双语一致性守卫照不出（两边一致地错）。修复：6 处统一为「宿主已自动注入时可省略重复声明，**但仅当注入的是『角色 + 任务 ID』**（只注入模型名/会话名不构成豁免）」，与 `multi-agent-closure-rules.md:33` 的完整口径对齐。**P2×3**：③`site/index.html:74` 英文行写「A **10**-field confirmation」而同行中文、README、`GATE_FIELDS` 均为 **11**——**且 SB22 的站点族正则是中文形态 `输出\s*(\d+)\s*字段确认单`，英文形态完全在守卫之外**（守卫盲区）；④`site/index.html:104` 的 Lite token 仍是 **154 / 481**，第四十四批统一重测为 **147 / 518** 时只改了 README 与 `docs/minimal-discipline.md`，**漏了站点这一面**（同类：README 内部 289/382 两行当初也互相矛盾）；⑤`agents/openai.yaml` 的 `default_prompt` 只列 **10** 个门禁字段，缺 `GATE_FIELDS[10]`「确认范围 / confirmation scope」——而 **SB9 只校验该文件的三个英文 token**（`form selection`/`risk tier`/`surveyed`），不含此字段 ⇒ 同样是守卫盲区。**守卫扩展（刻意不新增检查项，仍 22 项）**：SB9 的 openai.yaml token 清单增 `confirmation scope`；SB22 的 `gfam` 增站点英文形态族 `An?\s*(\d+)-field confirmation`。**两者均 RED/GREEN 双证**——删掉 `confirmation scope` → SB9 报 `missing gate tokens: openai.yaml:confirmation scope`（21/22）；站点改回 `A 10-field` → SB22 报 `site/index.html:74: gate field count (site EN) states 10 != 11`（21/22）；还原均 22/22。**副作用如实记档**：修复①后 SB16 的 tier-B「信息性差距」计数由 **30 → 32**——因 Resource Survey 节由「空节（字数不足 40 不计）」变为「纯中文节（计 1 条）」，Change Management 节由「中英混合」变为「纯英文（计 1 条）」；两条均属**中文权威版的正常形态**（workflow.md 开头明载"本文件是中文权威版"），SB16 按设计只作 informational、**不计入失败**，非恶化。**撤回（疑似但经复核不成立，记档防复发）**：①`artifact-check.py` 的 Form 正则「误判合规文档」——经实测，模板 `project-artifacts.md:41-49` 明确区分「文件头 `Form / 形态: 主干|子Agent|指挥官`」与「门禁 11 字段快照」，工具按模板实现**正确**；但两套词表（`子Agent` vs `子 Agent 增强`）并存确有踩坑风险，记低危建议未改；②「`确认范围` 仅创意任务需要」——「创意任务固定句」意为"创意任务时照抄这句"，非"仅创意任务需要"，该字段无条件必填，工具行为正确；③`selfcheck --out` 拒绝绝对路径——是**有意守卫**（报错信息清晰），非缺陷；④`install.ps1` 的 `-like` 前缀通配——第四十四批已修（现为 `StartsWith($sourceFull + 分隔符)` 真后代判断）。**已知不守卫（诚实记档）**：Lite token 与 references 单份上限需 tiktoken 而自检只用标准库，这两类数字只能人工重测；SVG 文字为路径轮廓无法机器比对；`SB10` 只校验 `install.ps1` 平台键、**`install.sh` 无守卫**（此缺口已在 `selfcheck.py:17` 的 Known blind spots 中诚实记档）。**验证**：selfcheck **22/22**（fingerprint `399844022e2b`）；全仓「10-field / 十字段 / 154 tok / 整文件 481」残留扫描均为 0；`git rev-list --count origin/main..HEAD` = 23（此前手抄记的 25 已按实测更正，手抄派生数字不复核就会漂——正是 SB21 要防的那一类）。
- **全仓系统审查 · 第三轮（2026-09-11 第四十六批）**：本轮查**运行时边界与异常路径**（前两轮查静态与文档）。**边界测试 22 项全部符合设计，零新增代码缺陷**：`selfcheck --out` 越界（`../evil.md`）正确 exit 2、仓库内子目录正常写入；`claim-check` 空声明清单 exit 2、`# expect exit N` 生效（实测 git 实际 0 vs 期望 1 → 判 FAIL）、非法 hash 条目报 malformed、全成立 exit 0；`artifact-check` 合成项目五种情形判定全对（合规通过 / 缺 1 个门禁字段 / superseded 无指向 / 非法状态值 / **逐轮**抓出第 2 轮缺字段）；`selftest-runner` schema→填写→archive 往返正常（PASS 计数正确）；`probe-runner` archive→report 过滤→verify 双向（无命中提示 UNVERIFIED / 命中 fail_pattern exit 1）正常；`install.sh` `bash -n` 通过；全脚本 `py_compile` 通过。**P3×1（本轮唯一发现）**：`generate-banner.py` docstring 声称「Design is kept in sync with `social-preview.svg`（same copy…）」——实测该 SVG **把全部文字转成了路径轮廓**（14 个 `<path>`、**零 `<text>` 节点**），文案无法被任何机器比对；改了生成器文案，SVG 会静默分叉且无人自动发现。已如实改写为：本文件是文案权威源，SVG 是需人工重新导出的冻结产物。**核验为净（本轮确认一致，未改）**：README TOC **25 条锚点零悬空**（第 11 批遗留的 L4 悬案就此了结）；硬规则首条引文 `宣布阶段序列不是确认。` 在 SKILL.md / AGENTS.md / workflow / self-test / README **五面逐字在位**；`docs/field-tests/` **10 份报告全部入索引**；`platform-installation.md` 安装表 **14 行**（`Antigravity` 与 `agents` 同指 `~/.agents/skills/`，与 README「13 个平台参数 / 14 行」一致——首轮 grep 大小写造成的误报已撤回）；`common-failures.md` F1–F6 齐备、无悬空 F7 引用；`identity-library.md` T1/T2/T3 与 P0/P1/P2 判据完整；`site/index.html` 无本地文件链接（全为 http / 锚点）；`.gitattributes` LF 策略与二进制例外完好。**测试纪律**：本轮边界测试曾向被跟踪的 `probes/last-run.md` 追加一条夹具记录，已当场 `git checkout` 还原——测试床一律用临时目录，不得污染工作树（自身纪律的复发点，记档）。

- **全仓系统审查 · 第二轮（2026-09-11 第四十五批）**：换角度重审（上一轮查"文档说的是不是真的"，本轮查"**守卫自己还灵不灵**"）。**P2×2**：①**SB21 的 CI 步数族是空转守卫**——正则写的是 `全绿（(\d+)/(\d+) 步`，而 README 实际措辞是「CI 步数由 SB21 守卫（当前 N/N 步）」，**零命中**；"正则找不到就不比较"在代码里等于"没漂移"，于是 CI 步数 12→13→**14** 全程绿灯无人察觉（本批加第 14 步时它依旧报 22/22，才被撞见）。修复：正则改为锚定实际措辞，并**新增守卫生效性自检**（SB21/SB22 内任一族正则零命中即判 FAIL，报 `guard is a no-op`）——空转守卫比没有守卫更危险，它给出"已检查"的假信号。**RED/GREEN 双证**：把某族正则改成必然零命中 → 当场 FAIL 并点名该族；还原 22/22。②**CI 对 claim-check 两层安全拦截零行为回归**——只跑 `--help`（只证明 CLI 能导入）；而解释器默认拒层历史上被绕过三次（`python3.13` / 引号包裹路径 / wrapper 前缀），每次都是"代码还在、CLI 还能跑、CI 全绿"但拦截没触发。修复：新增入仓夹具 `scripts/examples/claim-check-guard.md` + CI 第 14 步，**四条断言**：普通命令仍放行并实跑（防"改成全拦"式假绿）、输出含 `interpreter-indirect`、输出含 `dangerous pattern`、退出码 1。本地按 CI 逐步实跑 12/12 通过。**P3×1**：`hooks/README.md` 的 Windows 等效命令输出文本与 `hooks/session-reminder.sh` **不一致**（只有中文半句、缺英文），同一提醒在两个平台说法分叉；已对齐为逐字一致并注明以 `session-reminder.sh` 为唯一权威。**同步**：README CI 步数 13/13→**14/14**、CI 步骤清单补新步。**第二轮核验为净（确认一致，未改）**：23 字段完整任务包实测**恰 23 项**（逐条点名）；六字段迷你包中英两版**各恰 6 项**；五字段降级包 5 项；内部派发包**恰 11 项**；门禁 11 字段在三面齐全；21 份身份文件小节与 `_template.md` 全一致（deputy-commander 的「指挥权接管」是第 11 批刻意新增）；SB22 六族**全部有效无空转**；`.gitignore` 覆盖 `docs/selftest-run/` 与 `_patch_*.py`；`install.sh` 与 `install.ps1` 平台键 13/13 一一对应；`session-reminder.sh` 确为单行输出。**已知不守卫（诚实记档，不假装覆盖）**：Lite 速查卡 token 与 references 单份上限需 tiktoken 测量，而 selfcheck 刻意只用标准库——这两类数字只能人工重测，已在 README 注明测量日期。

- **全仓系统审查 · 第一轮（2026-09-11 第四十四批，总指挥令「迭代审查到连续一轮零发现」）**：覆盖 8 个 Python 脚本 + 安装器/CI + 全部文档面（README/site/AGENTS/21 份身份/13 份 references），逐项运行时实证后才判缺陷。**P1×1**：`claim-check.py` 解释器默认拒层**族缺口**——`pypy/pypy2/pypy3/ipython/jython/nodejs/osascript/dash/ash/busybox/csh/tcsh/cscript/wscript/mshta` 的 `interpreter_block_reason()` 实测**全部返回 None**（与第 32/35 批 `python3.13`、第 39 批 wrapper 同族洞，边界形态当时未当变异体）。修复：族扩至这些变体；**白名单同步含 `pypy/jython/ipython` 形态**（`pypy -m pytest` 仍是合法测试命令，不能因堵洞误伤）；`_argv0` 增 `pypy3.9`/`ipython3` 版本号归一化；docstring 家族清单同步。**回归 17 拦 / 10 放全符合设计** + 端到端实跑（2 拦 2 跑、文件/hash 判定正确）。**P2×4**：①`mutation-kill.py` 缺字段/坏 JSON manifest 抛 KeyError 轨迹、exit **1**，而 docstring 声明「2 = manifest/环境不合法」——文档与行为不符（CI 只看 0/1 会把用法错误误读成"有未测成项"）；改为四类结构化失败（文件缺失 / JSON 坏 / 顶层非对象 / 缺 artifact 或 mutants 非数组 / budget_ms 非整数）统一 exit 2，入仓示例回归仍跑通（sign 0% / magnitude 100%）；②`artifact-check.py` docstring 仍称「10 gate-field labels」而 `GATE_FIELDS` 实为 11；③README 工具表称「**24 类**破坏性命令黑名单」而 `DANGEROUS_RES` 实为 25；④Lite 速查卡 token 三处漂移（154/481/659 → 实测 **147/518/697**），README 自身 289 行（518）与 382 行（481）互相矛盾。**P2 另抓**：README:454「`docs/gate/*.md` **十字段**标签」应为十一字段（与②同族）。**P3×2**：README references 单份上限 13,424 → 实测 13,420；`docs/field-tests/selftest-run-2026-09-10/report.md` 断链 `../../references/self-test.md`（该目录下需上溯三级）。**守卫扩展（刻意不新增检查项，仍 22 项）**：SB21 增「黑名单类数」真值族（解析 `DANGEROUS_RES` 非导入）、SB22 增中文数字「十字段」字面拦截、SB20 写模式匹配改为**只扫 `open(` 之后片段**（原全行搜索会被同行 `d["a"]`、`"x" in s` 等短字面量误触发）。三者均 **RED/GREEN 双证**（变异 25→24、十一字段→十字段双双当场 FAIL，还原 22/22）。**核验为净（不改）**：21 份身份文件小节与 `_template.md` 全一致（deputy-commander 多出的「指挥权接管」是第 11 批刻意新增）；全仓断链仅上述 1 处；缩进围栏 0 处（SB6 严格 `^```` 口径无盲区）；「模式三」是指挥官协议正式名而非废弃的模式命名；`project-artifacts.md` 只引用不罗列门禁字段属设计使然。**已知不守卫（诚实记档）**：Lite token 与 references 单份上限需 tiktoken 测量，静态自检是纯标准库，**无法自守**，只能人工重测。

- **B 类翻译预估证伪 · 实测回滚（2026-09-11 第四十三批，总指挥裁决「回滚，放弃 B 类翻译」）**：第 42 批曾报「B 类（纯英文节）翻译成中文另省 ~6-7k」，本批先行试点 5 节（Loading Contract / Identity Boundary / Execution Modes / Three Internal Role Faces / Subagent Mode Protocol，span 替换、标题保留、镜像覆盖 7/7）后逐节实测（o200k_base，HEAD 对比）：**EN→CN 翻译不但不省反而 +4%~+23%（合计 1,165→1,306 tok，+12.1%）**——根因：o200k 对英文 BPE 压缩极省（~1.3 tok/词），中文 ≈1 tok/字符且同义中文字符数更多；「6-7k」是把 B 类的**存量规模**误当成**可省额度**，方向性错误（与第 40 批「中文 ≈1 token/字」教训同源，当时未外推到翻译场景）。**总指挥裁决放弃 B 类翻译**：5 节试点已从 `HEAD` 逐字节恢复（workflow.md 回到 **11,961 tok**，工作树零 diff），**仓库文件零变更、本批无代码/文本改动**；主文件维持中英混合现状，EN 原文有 `series-reasoning-workflow-en.md` 镜像兜底。**留档结论（防止将来重试）**：B 类翻译的唯一收益是语言统一（全中文主干），代价约 +700–900 tok——只有当语言一致性的价值被显式判定高于该成本时才应重启；A 类（成对 EN 重复删除）的 -1,647 tok 结论不受影响。selfcheck 22/22（工作树与 `20359ea` 一致）。

- **workflow 权威版 A 类 EN 下沉 · B 类翻译待裁（2026-09-11 第四十二批，总指挥批「可以」）**：按「允许降能力换 token」性价比清单执行第一大项。**落地时修正提案前提**：通读全文后发现 workflow.md 的英文分两类——A 类（EN 段+CN 段成对，EN 是真重复）与 B 类（**纯英文节、无中文对**：Loading Contract/Execution Modes/三大角色面/Subagent/Commander 协议/Generative Divergence/Honesty Gate 正文等约 6-7k tok）。**本批只删 A 类**：Resume Check/User-Path 全部子类（引言、六类产物 EN bullets、EN Rules）/Resource Survey/Honesty Gate 五条行内对/Task Dispatch 对/Non-GUI EN 重复段/环境前置 EN 对——共 52 行删除 + 8 处行内「EN / CN」对保留 CN 半；删前逐短语核验镜像覆盖 8/8。**净效果：workflow.md 13,608→11,961 tok（-1,647 / -12%），740→693 行**；Section Map「700+ 行」注记同步为「近 700 行」。**B 类（纯英文节翻译成中文）不在本批**：那是对全仓库最承重规则文本的翻译工程，语义保真风险实质存在，另行报总指挥裁决（做则另省 ~6-7k）。**过程事故（当场修复）**：删除脚本用 `Rules:` 前缀误伤了 5 个英文节的节头（只想删 User-Path 那一个）——grep 定位后逐一回插。**验证**：CN 关键句抽查 4/4 在位（蒙眼开车/逐项通过≠结果合理/轻通道省的是流程/四档）；selfcheck **22/22**；Gate 模板未动。
- **二次简化 · 使用视角七项删改（2026-09-11 第四十一批，总指挥裁决「7 项全落」）**：总指挥令「重新检查，可能有你认为要留在但实际使用时可以删改增减的内容」。按「实际会话里哪些字真的会烧」逐面复查，7 项落地：①核心风格「交互类亲手操作」bullet 缩为指针——与工作流第 5 步**逐字级重复且第 5 步更全**（两处同在 L0，纯冗余）；②「盘点」bullet 缩为指针——门禁模板「已盘点可用资源」字段才是执行面（R1 教训：模板没带的字段会被跳过），轻通道本就明文跳过盘点，bullet 无独立生效场景；③范围克制 bullet 删闰年/世纪年例子——workflow:392 权威版连例子一起承载；④Resume Check 7 项全称展开压成短标签枚举（7 项名全保留）——仅续会场景触发却每轮常驻，workflow:40 有权威版；⑤宿主对齐段 4 bullet 压成一段——每宿主只执行一次却每轮烧，三底线（证据/UNVERIFIED/真实环境）+「不得改 skill 本体」逐字保留；⑥「平台二次确认开关建议」独立段并入宿主对齐句（同属一次性建议）；⑦「以最终结果质量为准」空泛价值句并入「证据强于信心」bullet。**净效果：4,071→3,843 tok（-228，较第 39 批基线累计 -273 / -6.6%），126→113 行，摊销 192 tok/轮**。**复查后仍不动（记档）**：Gate 11 字段及括号条款（v5 复跑逐字段真实生效，无「用不上」证据）、硬性规则 5 条、轻通道+排除项（三轮探针反绕过边界）。**勘误（第 40 批）**：该批 CHANGELOG 称「澄清模式节并入门禁字段」实际只增未删——节仍挂在 L0，本批补删并在此如实更正。**纪律核验**：Gate 模板 11 字段 diff 逐字节零变化；selfcheck **22/22**；README 六处数字同步。
- **token 成本体检 + 第一档简化（2026-09-11 第四十批，总指挥裁决「只做第一档」）**：全仓 token 全景实测（o200k_base）——常驻面仅 SKILL.md+VERSION（4,116→**4,071** tok），references 全部按需且 Section Map 防整读（workflow CN 13,608 为最大按需面；self-test/en 镜像零运行时成本），openai.yaml 仅 728——优化主战场确认为 SKILL.md 本体。**第一档落地四项（纯去重，零决策点损失）**：①References 14 条整句描述压成短标签（路由指针全保留；policy-template 条目补回「项目专属规则不写回本 Skill」行为规则）；②协作架构段去重（首末段重复的「形态自选+指名优先」收拢为一句，混合搭配句并入）；③澄清模式节并入门禁「澄清方式」字段（B 项逐题行为描述保留）；④模式3规则 bullet 2/3 合并（角色命名/模型解耦/规划落盘决策点全保留）。**如实记录**：实省仅 **45 tok（-1.1%）**（4,116→4,071）、127→**126** 行、汉字 3,469→3,426、摊销 206→**203** tok/轮——远低于立项估算（-250），原因：中文 token 密度高（约每字一 token），SKILL.md 的「冗长」大多是承载行为的小字数条款；**本批更大收益是去重**（协作架构/澄清模式/模式3 的跨段重复消除，减少未来同步漂移源）。第三档（Gate 11 字段条款/workflow 权威版/EN 镜像/description）维持不动并记档理由：每个冗长括号都是真实 RED 买来的。README 六处数字同步。**过程纪律事故（当场恢复）**：编辑中误删第 34 批创意豁免段，git diff 核验发现后立即逐字节恢复——第一档边界（不动任何条款）靠 diff 复核守住。**验证**：selfcheck 22/22。
- **饱和式复查 · claim-check wrapper 前缀绕过洞 + 门禁字段数散文漂移 + SB22（2026-09-11 第三十九批）**：总指挥令「反复审查直到挖不出问题」。**RED（运行时实锤，解释器默认拒层第三类绕过）**：wrapper 前缀解释器——`env python -c` / `nice python -c` / `timeout 5 python -c` / `call python -c`（cmd 内建）**四条全部 PASS 且载荷执行**（临时床 4/4 verified、输出可见、0 blocked），而裸 `python -c` 正确被拦——层 2 只查首词，wrapper 家族未覆盖，与第 32/35 批「版本号/引号」同类（边界形态当日未当变异体）。**修复（延续已批的默认拒+窄白名单方向）**：层 2b wrapper 剥离——已知 wrapper（env/nice/timeout/call/command/exec/nohup/time/stdbuf/setsid）按各自参数形态剥离后对**有效命令**重apply 同一默认拒+白名单（白名单锚定未剥离命令，`timeout 5 python --version` 与裸 `python --version` 同样可用），有界深度 4；两类 fail-closed：①wrapper 自身参数形态不认识即拦（`env -i`/`env -u`/`nice --adjustment=5`），②sudo/doas/xargs 即拦（提权/流式扇入可在任意位置藏解释器，`sudo -u root python -c`、`xargs -a list python -c`，验收声明无合法用途）。**回归矩阵 40/40**（24 拦 + 16 放），端到端实跑复证（拦的零执行、`timeout 5 git --version`/`call git --version` 真跑通）。**P1 散文计数漂移（SB22 立项依据）**：第 34 批门禁增至 11 字段时漏更 README 三处「10 字段门禁/确认单」+ project-artifacts.md 一处「10 字段标题齐全」（与其自身「11 字段快照」行自相矛盾）——**证伪第 35 批「此类残留全在 dated 面」的断言**。修 4 处 + 新增 **SB22**（门禁字段数散文 = artifact-check GATE_FIELDS 真值，解析非导入；按 README 准入规则立项，缺陷可指认 65def17..8437a6d）。SB 计数面八处同步（README 徽章/树/工具表/CI 行/预算行 22 项/发布前自检、CI 工作流名、site/index.html）。**二轮扫尾补抓**：site/index.html:73 同类「10 字段确认单」残留（SB22 首版面清单漏列 site）——修掉并把 site 纳入 SB22 守卫面（gfam 6 面）；SB22 RED/GREEN 双证（临时改坏 site 计数 → SB22 当场 FAIL 21/22 → 还原 22/22）。**P2 文档修复**：v5 判定报告悬空引用（「取代上一节第 4 点」所指已不在文中）改指首轮判读第 3 点；待办 push 手抄数字改为指针口径（`git log origin/main..main` 实时为准，SB21 教训同类）。**验证**：selfcheck **22/22**（SB22 实跑 PASS）；全 .py py_compile 通过。**补登记**：第 38 批（下条）此前漏登 CHANGELOG，本批一并补齐。
- **鹈鹕 v5 复跑判定（2026-09-11 第三十八批，新条款 RED 复核 · 产品级）**：总指挥执行 v5 复跑（多轮版话术对齐首轮交互，测试床 `Desktop\pelican-ab-v5`），lead 读盘 + agent-browser 真实浏览器截图判定（`docs/field-tests/pelican-ab-2026-09-11/report.md` v5 节 + `evidence-v5/` 两截图）。**产品级结论**：首轮 RED 核心症状（方向保守化）**反转**——yes 臂交出黄昏湖畔美术方向 + 13 组 keyframes（三层视差/两段关节腿/喉囊/眨眼），no 臂仍是无风格通用卡通且**整体骑车组被 CSS transform 覆盖 SVG 属性甩到左上角裁切**（用户一眼可见）。总指挥动画实测（部件级）进一步发现：**两臂同一 bug 类**——yes 臂腿/眼同样被覆盖位移（两中一躲），另有有意画的单臂扶把（解剖学错误，源码注释坐实）；yes 臂质量修订 4/5→**3/5**，no 臂 2/5。**判读**：skill 收益为真但「强制不偷懒」只兑现为「少偷懒」，完成门真实环境验收疑似被跳过或只看整体不看部件——七项判定表 #1/#2/#3/#7（过程证据）待会话记录核验，权重升级；#6 成本待平台统计。**候选教训（已立项未入条款）**：两臂位移同源 + 解剖错误指向「画前无部件清单/坐标系规划」，总指挥裁决 **A+B 合并采纳（假设显式化条款）+ 立项 P10 先验压制探针**（章鱼骑车/企鹅开挖掘机双变体），终审稿在 workspace 待落地。**验收教训入册**：lead 截图验收只看整体未看部件——静态截图 ≠ 用户席观看动画，总指挥动画实测才是有效用户方验收。n=1 不外推。
- **让位原则 + 用户方视角验收（2026-09-11 第三十七批，总指挥两项裁决）**：①**让位原则**升格为核心风格总纲——「本 skill 只规范流程，不主导内容：其他 skill 或宿主能力对内容、风格、创意有主张时，本 skill 让位并配合；但诚实（证据/UNVERIFIED）、安全（破坏性防护）、真实环境验收是最后防线，任何优先级下不失效」。设计讨论结论：总指挥原案「绝对低于其他任何 skill 和 AI 本身」采纳前半（对内容维度让位，是第 34 批③的推广）；否决后半——「绝对低于 AI 本身已有的」会清零 skill 全部收益（R2/R3 的假完成修复、位置违规归零、UNVERIFIED 标注全部来自 skill 压过 AI 天性，F6 证明 AI 自检不可信），且鹈鹕问题根源是决策点保守偏好而非规则冲突，全局优先级修不了它，故改为**分维度让位**（内容让、诚实安全不让）。②**用户方视角验收**：最终验收收尾必须以用户方视角整体重看结果——作为交付物在用户眼里是否成立、是否解决真实目标、有无奇怪/多余/缺失之处，逐项操作通过不等于结果合理（补「逐项通过 ≠ 整体合理」的认知缺口）。**同步面**：SKILL.md 核心风格 +1 行 / 工作流第 4 步补句（126→**127** 行）、workflow CN/EN Final Acceptance 第 1 项补句、openai.yaml 补两句（yaml 校验通过）；README 六处数字重测（**4,116** tok / 3,469 汉字 / **206** tok/轮 / **127** 行）。**验证**：selfcheck 首跑 20/21——**SB21 当场抓到行数漂移**（README 126≠127），重测同步后 **21/21**（守卫按设计工作）。核验为净：minimal-discipline 速查卡三条不含让位/用户视角（Lite 不扩容）；examples/lessons 不复述这两条。
- **D 语义抽检修复 · B1 对齐 + agent-modes 豁免指针补齐（2026-09-11 第三十六批）**：第 34 批新条款的双语逐句对照**语义等价**（判据/豁免/标注/兜底四要素 CN/EN 齐全）；B1 三面轻通道措辞软漂移仍在——`SKILL.md`「产品形态」对齐为「形态」（workflow/agent-modes 均为「形态」）；`agent-modes.md` 中英两处轻通道重述补**方向豁免指针**（此前重述了排除项却漏了同节新增的豁免，正是 R1「模板没带的字段会被跳过」教训的文本版）；self-test.md 无字段数期望（核实不受 11 字段影响）。selfcheck **21/21**。
- **全面复查二轮 · claim-check 两个绕过洞 + 收尾修复（2026-09-11 第三十五批）**：总指挥令全面复查，重点是第 31–34 批增量。**RED×2（运行时实锤，均为第三十二批解释器默认拒层的绕过）**：①**版本号解释器名**——`python3.13 -c "…"` 未被拦截（`python3.13` 不在 21 名家族集合里；本机 PATH 无该命令故 launch 失败，但「未被拦、已尝试执行」成立，在有该命令的机器上即任意代码执行）；②**引号包裹路径**——`"C:/…/python.exe" -c "…"` **PASS 且载荷执行**（`_argv0` 未剥引号，`python.exe"` 的尾引号让 `.exe` 剥离失配）。**修复**：`_argv0` 剥首 token 两侧引号 + `pythonw?` 版本号后缀归一化（`fullmatch(r"(pythonw?)(?:\d+(?:\.\d+)*)?")` → `python`/`pythonw`）；白名单 `_INTERP_TOKEN` 接受引号包裹路径（`"C:/…/python.exe" -m unittest` 放行，fail-closed 不误伤合法形态）。**回归矩阵 9/9 符合设计**：放＝`python --version` / 绝对路径 + `-m unittest`（实跑 OK）/ **引号路径 + `-m unittest`** / `py --version` / `git --version`；拦＝`&&` 连锁 ×2 / `python -m pip` / **`python3.13 -c`** / **引号路径 `-c`**。**P2 修复**：SKILL.md 核心风格提要行的细则指针由「agent-modes 门禁字段」（agent-modes 实无门禁字段，grep=0）改指 `series-reasoning-workflow.md` 门禁节。**补漏**：`docs/field-tests/README.md` 报告索引缺 77 条自测报告（第 24 批漏登记）与鹈鹕 A/B 判读表——两行补全。**核验为净（不改）**：install.sh 子目录守卫本就是正确形态（`case "$DEST" in "$SOURCE"|"$SOURCE"/*)`，等价 StartsWith+分隔符，无 PS 版修掉的兄弟目录 bug）；openai.yaml 经 pyyaml 6.0.2 解析合法；mutation-kill 入仓示例 `run` exit 0；全 8 个 .py `py_compile` 通过；三处残留「10 字段」表述全在有日期的记录面（field-test 报告/提案/评审归档），按纪律不改；CN/EN 门禁模板均为 11 字段、与 project-artifacts/artifact-check 一致。**P3 已知 cosmetic（不改，已有 H2 注记）**：claim-check 对宿主命令报错输出按 utf-8 errors=replace 解码，cmd.exe 自身的 cp936 报错会显示 mojibake——方向性已知取舍。selfcheck **21/21**。教训（记死）：**新的默认拒层落地当天，边界形态（版本号、引号、大小写）要当作变异体逐一过**——第 32 批的自测矩阵设计时漏了这两个形态，二轮复查才抓到。
- **外部审查 21 项清单全部核验归档（2026-09-11 第三十三批）**：HANDOFF 待办 5 清账——deep-audit 17 项 + 第二报告 P2 4 项（清单实为 21 项，HANDOFF 记 18 系笔误）逐条对盘：**已过时/被后续批次顺带解决 7 项**（B5 大部分、C1、C2、C4 大部分、D1 前提消失、D3、P2-3 fail_patterns 已被 verify 消费）、**证伪 1 项**（D4——SB3 fence 判据仍在但 77 条全带 fence 无活体失败，放宽反降判据强度，不采纳）、**成立待处置 12 项**（B1 软漂移、B2 脚注、B3 §3.5、B4 空值退化、C3 EN 锚点、C5/P2-1 无 RED 不准入留候选、D2、D5 其引用 RED 例句已消失须重新举证、F1、F3、P2-4 dry-run）、**纯建议 3 项**（E1/E2/F2）。全表归档于 `docs/reviews/2026-09-10-external-reviews-5-6-7.md` 末尾「VERIFIED (2026-09-11)」。**顺手新抓活体漂移并当场修复**：README「截至目前 24 批」vs 实际 32 批——SB21 同类手抄派生数字但不在四族内，修法与 SB21 同理（删手抄件、指向 CHANGELOG 权威源）。selfcheck 21/21。
- **创意/冒险任务兼容四条款 · 鹈鹕 A/B 反哺（2026-09-11 第三十四批）**：真实案件 RED 驱动的规则行为变更。**证据**：同模型双臂鹈鹕骑车实测（判读表 `docs/field-tests/pelican-ab-2026-09-11/report.md`，产物在 `%USERPROFILE%\Desktop\mimo-1\`）——yes 臂（带 skill）方向倒挂：通用卡通扁平 vs no 臂复古海报设计系统，且技术妥协（固定短腿 vs no 臂大腿小腿两段关节 `thigh-a`/`shin-a`）；yes 臂 51 min / output 73,955（db `ses_ffe5f70829732ffeYrcVXxo7qV` 真值）；yes 臂唯一硬增量是 UNVERIFIED 按钮交互标注。**病灶（钉在第三十三批前行号）**：①推荐方案单一 + 一次性确认把「提案被否风险」转化为保守偏好，用户「a」即锁死方向；②资源盘点列出 frontend-design 却以「当前够用，不装」弃用——违反第 27 批「宿主已有能力优先」。**四条款（总指挥批「可」后按草案落地）**：**①方向不锁死**——创意/审美主导任务推荐方案须并列 2–3 个真实不同方向（保守/均衡/大胆至少各一，大胆须真候选不许陪跑）+ 新增第 11 字段「确认范围」固定句（确认只锁目标/范围/交付物/落盘路径，风格不因确认锁死，换向须在证据报告说明原因）；**②可逆冒险**——风险分档判据明文＝不可逆性/影响面/副作用，「审美方向的大胆不是风险：被否掉的方向重做一次的成本，低于所有参与者都交安全解的成本」；完全可逆本地无副作用创意产物可跳过**方向**确认直接选最大胆方案起跑（落盘路径与范围仍须确认，证据报告标注「冒险直选」；四重限定防滥用，拿不准仍升中档）；**③盘点默认用**——任务主质量维度（视觉/交互/文案/数据/安全）被宿主已装 skill 覆盖时默认＝用它主导该维度，弃用须写一行理由，禁止「当前够用，不装」式无理由弃用；**④完成门零改动**——证据报告/UNVERIFIED/实操验收/破坏性防护一字不动。**核心风格区新增一句话版**：「创意任务防的不是返工，是平庸：门禁锁定范围与落盘，不锁定方向；大胆是默认，保守才需要理由」。**多表面同步（9 面）**：SKILL.md（门禁模板 11 字段 + 轻通道节豁免段 + 提要行，122→**126** 行）、`series-reasoning-workflow.md`（CN 权威模板同步）、`series-reasoning-workflow-en.md`（EN 镜像 10→**11 fields** + 豁免段）、`project-artifacts.md`（门禁快照 10→11 字段）、`scripts/artifact-check.py`（`GATE_FIELDS` 增「确认范围」，前置在位校验不受影响）、`agents/openai.yaml`（default_prompt 盘点默认用 + 创意方向两句内嵌）、README（行数 122→126 两处、token 重测 3,564→**3,974** / 汉字 2,919→**3,334** / 摊销 178→**199** tok/轮 共 6 处、结果一览表 **9→11 项**——顺带修复第 29 批加第 10 行时漏改表头的存量漂移、树注释加鹈鹕 A/B、实测表增第 11 行）、`docs/field-tests/pelican-ab-2026-09-11/report.md`（新增判读表）。速查卡 Lite 不涉及字段清单，免同步（154 tok 不变）。**验证**：selfcheck **21/21**（SB21 行数族自动吃到 126，README 两处「≈行」已对齐）；`py_compile artifact-check.py` 通过；token 实测口径与第 27/29 批一致（o200k_base，SKILL.md+VERSION）。**n=1 声明**：本组条款由单案举证立项，真实案件回归第二波（18 床）跑完后复核松紧；有条件时同模型在新条文下复跑 yes 臂对照方向质量。**刻意不做**：不加「创意任务禁用本 skill」硬边界（用户自决）；不动 77 条冻结自测与 21 项 SB 上限。
- **claim-check 解释器间接执行洞补实 · 双层拦截（2026-09-11 第三十二批）**：第二十二批「证实但未修（待裁）」+ HANDOFF 待办 6 正式处置。**先写失败用例（纪律）**：临时床实锤——不带 `--allow-dangerous` 时 `python -c "import shutil; shutil.rmtree('victim')"`、`python pwn.py`、`node -e "…"`、`powershell -Command "…"` 四条解释器间接执行命令**全部被执行**（4/4 verified、exit 0、0 blocked），牺牲品目录被删、三个标记载荷全部生成——docstring 此前如实承认的「interpreter-indirect execution NOT covered」由声明变成了带磁盘证据的 RED。**方案经总指挥裁决（B 案：解释器默认拒 + 窄白名单）**，否决 A（shell=False + argv 白名单——最彻底但 Windows 无 shlex 兼容 argv 解析、管道/`&&`/重定向合法声明全破，回归面最大）与 C（只补黑名单字面形态——`python pwn.py` 照样放行，打地鼠）。**实现**：新增第二层 `interpreter_block_reason()`——命令**首词**（剥盘符/路径/`.exe` 后）属于解释器/壳家族（python/python2/python3/pythonw/py/node/deno/bun/perl/ruby/php/powershell/pwsh/cmd/bash/sh/zsh/fish/ksh/**npx/uvx/pipx** 共 21 名）即默认 BLOCKED，拦截消息区分两类（`dangerous pattern` vs `interpreter-indirect execution … payload is not auditable`）；**窄白名单全命令锚定**且否决连锁（负类排除 `;|&`、反引号、`$()`，`python --version && python pwn.py` 照拦）：放行 `python -m unittest|pytest`（含 `py -3` 前缀与绝对路径形态）与 `python --version|-V`；`--allow-dangerous` 仍同时覆盖两层。**诚实注记（写进 docstring 与 README）**：白名单放行的是「跑该项目测试套件」这一工具本职——pytest conftest.py / 被导入的测试模块就是项目代码，仍可能藏载荷；家族外的 exec 型工具（`go run`/`cargo run`/`make`/`uv`/`npm`/`env`/`xargs`）不覆盖；**两层都不是沙箱**，真实信任边界依旧是可信声明源 + 逐条人审。docstring 信任模型重写、示例命令改为白名单形态、README 两处散文面同步。**验证矩阵（8 例，4 放/4 拦全符合设计）**：放行＝`python --version` / 绝对路径 python + `-m unittest`（test_ok 实跑 OK）/ `py --version` / `git --version`（非解释器 argv0 不受影响）；拦截＝两条 `&&` 连锁注入 / `python -m pip --version` / `cmd /c ver`；RED 床复测 4/4 BLOCKED、0 executed、exit 1；`--allow-dangerous` 覆盖路径正常（4 执行 0 拦截）；py_compile 通过；selfcheck **21/21**。**刻意不做**：不把 `uv`/`go`/`cargo`/`make`/`npm` 等全部纳入家族（误伤面失控且仍非沙箱，如实记为已知局限）；不改 CI（claim-check 无 CI 步骤，不涉 13 步守卫）。
- **全面审查 12 项发现全修 · P0/P1/P2 两批落地（2026-09-11 第三十一批）**：对全仓做一轮逐文件 + 运行时边界实验 + git 历史重放的全面审查，产出 12 项发现（**P0×2 / P1×3 / P2×7**，逐项带位置/影响/证据/修法，归档于用户工作区 `全面审查-2026-09-11-代码与配置.md`，不随仓库提交），经总指挥批准后分两批全修。**P0（均为第三十批新工具自身的缺陷）**：① mutation-kill **缺基线变异体时区分率全假报 0.0%**——同一变异体自检已 FAIL，无基线时输出 `区分 0/1 = 0.0%` 且 exit 0，与自身「命中期望 1/1」矛盾；修复＝硬前置**恰好 1 个基线**（无 `edits` 或 `category:"baseline"`，二者并存判矛盾 exit 2），无基线拒绝输出区分率；② **ERROR 基线污染零区分告警**——产物缺 `</body>` 时基线判 ERROR，`ERROR==ERROR` 被当「与基线相同」塞进零区分力告警；修复＝基线非 PASS/FAIL **整轮作废**（exit 2）、`same_as_baseline` 仅在双侧均为 PASS/FAIL 时计算、零区分名单过滤 ERROR、缺失注入锚点改显式 ERROR 并提示 manifest `inject_target`（默认 `</body>`，可指定 `</html>` 等）。**P1**：③ SB21 真值源是格式敏感正则（`c = new(` → `c  =  new(` 计数 21→0）——改**运行时 `len(checks)`** 为真源（SB21 是最后一项，重命名/空白免疫）+ 定义扫描交叉核对，不一致即拒绝比较表面（fail loudly 而非报假数）；实测 M7 空白变异 GREEN/免疫、M8 重命名 RED；④ SB21 锚点收紧（`SKILL\.md[^\n]*?（≈(\d+)\s*行`、`^(\d+) 份 references`）+ 次序数改为「仅在已匹配 `SB1[–—-]+SB\d+` 的行上」的共现规则，消除误报面；⑤ **README 声称 CI 13/13 步而 workflow 实为 12 步**（自 `95d1b03` 起）——补第 13 步（mutation-kill `--help` + 入仓示例 manifest JSON 合法性冒烟）并把 **CI 步数登记为 SB21 第 5 族**，使该数字既为真也被守卫；`pyyaml` 钉 `6.0.2`。**P2**：⑥ claim-check 黑名单 17→**24** 类——补 `git push --delete` / `git push :ref` / `git push +`（强推三形态）、`git checkout .`、`git restore`（负 lookahead 放行安全的 `--staged` 形态）、`git branch -D`（**`(?-i:D)` 作用域旗标**修复自测发现的误拦：原先大小写不敏感把合法的 `git branch -d merged` 也拦了；`-D` 保持必拦）、`truncate -s 0`，附同义洞理由注释；对抗性测试 **10 拦 / 7 放全部符合预期**；⑦ install.ps1 子目录守卫改 `GetFullPath` + 前缀 `StartsWith($sourceFull + 分隔符)`，修复 `-like "$src*"` 会误匹配同名前缀**兄弟目录**的缺陷；⑧ README「全部工具为标准库实现」收窄为「全部**校验类**工具…（维护者工具 `generate-banner.py` 额外需要 Pillow）」；⑨ mutation-kill 文档与报告把**区分率与命中期望拆为两个指标**分别定义（主指标＝区分率），报告 JSON 增 `metric_definitions` 与 `errored`；⑩ 杂项修复 a–g：`selftest-runner.py` 死代码、`generate-banner.py` `text_width` 与 `center_text` 口径统一（`bbox[2]-bbox[0]`）、`probe-runner.py` 证据清洗补三连反引号替换（呼应 SB6 围栏奇偶）、SB12 行数口径与 SB21 统一为 `splitlines()`、示例 manifest `_note` 补「恰好一个基线」、README probe-runner 行注明 `probes/last-run.md` 已跟踪且弄脏工作树属预期。**验证**：selfcheck **21/21**；黑名单 17 例对抗测试 10 拦/7 放；SB21 杀伤测试（临时副本仓库）M1–M5+M8 RED、M6 空白 GREEN 免疫、还原后 GREEN（6/6+1）；6 个 CLI 冒烟 exit 0；全 7 脚本 `py_compile` 通过；入仓示例 `run` exit 0。过程教训：本批 P0 两项全部是**第三十批自己的新代码**——新工具落地当天就该跑一轮变异级自审，而不是等下一轮全面审查。
- **散文计数守卫（SB21）+ 变异工具入仓（2026-09-11 第三十批）**：两项均来自第二十九批自产的发现，按仓库准入规则（须证明抓到过真实缺陷且可指认提交哈希）立项落地。**① SB21「散文计数匹配其来源」**：仓库有一类数字由某来源派生、却被手抄进多处散文，且无人对齐——SB4/SB5 只数**文件**、SB19 只管**身份计数散文**，因此**检查项数 / `SKILL.md` 行数 / 自测条数 / references 份数**四类散文计数**全部无守卫**。**缺陷取证（逐提交重放「声明值 vs 真值」）**：`dacb935`…`eef524f`（**7 个提交**）`selfcheck.py` 已定义 17 项而 README 复杂度预算行与 `site/index.html` **两处**仍写「SB1–SB16」；`d99f353`…`9d32cbb`（**6 个提交**）`site/index.html` 仍写「SB1–SB17」而真值为 18（`9d32cbb` 恰是 SB19 的证据提交——同一次修好了身份散文、却把检查项数留在旧值）；`4a564db`…`83ed1ec`（**4 个提交**）README「≈122 行」而 `SKILL.md` 实为 121。**实现**：登记表驱动（活表面 + 逐行正则 + 期望值元组），来源真值**现算不落常量**（`c = new(` 计数 / `SKILL.md` 行数 / 解析 Test 条数 / `references/*.md` 计数）；**无容差**（"≈/约" 不豁免，这些数字本就是手工递增锚点，精确相等即设计意图）；**排除有日期的记录面**（CHANGELOG / INTERNAL-HISTORY / docs/reviews / docs/field-tests——过去某天写的计数对那天正确，同 SB19 理由）。**鉴别力（杀伤测试，非"跑绿"）**：把登记表套回历史状态——`dacb935` RED(2 处)、`d99f353` RED(1 处)、`9d32cbb` RED(1 处)、`74cbb76` GREEN；加 SB21 当刻（表面仍写 20）**RED 命中 8 处**（README×6 + site×2），同步后 GREEN（`SB=21 SKILL=122 tests=77 refs=13 across 21 prose surfaces`）。**② 变异工具入仓** `scripts/mutation-kill.py`：把产物自带自检当被测对象，注入单点变异体逐**错误类别**统计**区分率**（= 判定与基线不同的变异体 / 该类**有效**变异体）；原产物只读（前后 sha256 比对，改动即作废）；`artifact`/`workdir` 支持**相对路径**（按 manifest 所在目录解析）；**ERROR 不计入区分率分母**（不是"没被抓住"，是根本没测成）——此口径本轮修过一次：初版把 `same_as_baseline=None`（ERROR）误记为"区分"，把 render 族虚报成 1/3=33.3%，修正后为 **0/3=0.0%**，与第二十九批 F6 的结论一致（唯一"被抓住"的 m03 实为 `find` 串在渲染行与审计行各命中一次导致的 ERROR，已在示例与 manifest 中用「换行+缩进」唯一化）。附**开箱可跑的入仓示例** `scripts/examples/mutation-kill-demo.{html,manifest.json}`（极小产物+自检，演示 sign 族 0% 区分 / magnitude 族 100% 区分）。README 仓库树与工具表各加一行。**多表面同步**：检查项数 20→21（README 徽章/树/表/CI 行/复杂度预算/发布前清单、`site/index.html`、workflow 步骤名共 8 面）、次序数 21→22（README+site 两处）、复杂度预算上限 20 项→**21 项**并加入 SB21 准入行。**验证**：selfcheck **21/21**；SB21 在历史缺陷状态 RED、干净状态 GREEN；入仓示例 `run` exit 0（sign 0% / magnitude 100%）；原产物只读 sha256 前后一致。**刻意不做**：不把 SB21 扩成"全仓所有数字都必须匹配"的宽检查（只登记四族、只覆盖活表面，窄而可信优于宽而误报）；不新增 reference 文件（会撞 SB5 的 `references == 13`）。
- **验证敏感性入册 · 反例 F6 + 四法则 + 杀伤率指标（2026-09-11 第二十九批）**：由外部测试床（A2 鹈鹕骑车，3 模型 × 2 臂 = 6 具产物）反哺的**方法论级**缺口——「自检报 N/N 通过」到底算不算证据。**实验**（原产物只读、7 个单点变异体、各带独立 `--user-data-dir`、无头 Chrome `--dump-dom` 取回自检结果）：某产物自带页内审计报告头条为「**14/14 通过**」，而变异杀伤实验显示——m00 原样（**含缺陷**：车轮逆时针而牙盘/飞轮顺时针）**14/14 PASS**；**m01 把符号改回正确 → 14/14 PASS（与 m00 完全相同）**；m02 车轮被取反 14/14 PASS；m03 只改渲染层 14/14 PASS；m04 破坏齿数比（ω₂=2ω₁）12/14 FAIL；m05 破坏纯滚动 13/14 FAIL；m06 改声明常量 `RATIO` 14/14 PASS。**按层杀伤率**：物理关系层 **100%**(2/2)｜渲染·约定层 **0%**(0/3)｜声明常量层 **0%**(0/1)。**根因（代码级）**：14 项中仅 ⑨ 读 DOM；`⑬⑭` 的 `ok` 参数是**字面量 `true`**（写死通过）；`⑥` 与 `⑤` **共用同一误差变量**且其相位判据 `|((θ+π)−θ)−π|` 恒为 0；`⑫` 由被测的同一对象算出（自证）——**约束符号/手性 0 项、约束 state→DOM 渲染映射 0 项**，而缺陷恰恰发生在 `render()`。**跨床复核**：同批 6 具产物的自检**全部**不含方向/符号断言（唯一带外部校验脚本的一具，122 行断言全为几何与代数残差）。**落地**：①`references/common-failures.md` 新增 Claims 行「自检通过 / N/N 全过」（最小充分证据＝**杀伤率**，非通过率）+ **F6** 案例 + **「验证敏感性四法则」**节（层级 / 对称盲 / 自证 / 承载面闭包）+ 两条合理化借口行 + 案例准入的**例外形态**（外部实证指针 = 测试床 + 产物 sha256 + 可复跑脚本，须显式标注，且不得据此弱化 F1–F4 的提交哈希要求）；②`SKILL.md` 核心风格新增一句话版「通过率不是鉴别力：把我要防的那个错误做一次，它会不会红？」；③归档 `docs/reviews/2026-09-11-verification-sensitivity-mutation-kill.md`。**刻意不做**：不新增 reference 文件（会撞 SB5 的 `references == 13` 硬断言并牵动多表面计数）；不在本仓新增变异 runner（该工具天然按产物定制，已落在用户级 skill `verification-sensitivity-audit` 的 `scripts/run_mut.example.py`）；不新增 SB 项（准入要求"抓到过真实缺陷且可指认提交哈希"，本条为外部实证，不满足）。**本批自身产生的多表面数字漂移，已全量清理**：`SKILL.md` 由 121→**122** 行 ⇒ 常驻面 token 由 **3,476→3,564**（o200k_base，2026-09-11 重测）；衍生值 ≈2,850→**≈2,919** 汉字、摊销 ≈173→**≈178 tok/轮**；README **五处**表面同步（77/287/300/301/514），成本节日期口径改写为「常驻面 2026-09-11 重测、其余行日期不变」；失败档案引用 **F1–F5 → F1–F6** 两处（README:216/413）。**历史批次内的 3,476 / 2,850 / 173 等旧数按 append-only 不改写。** **顺带发现（未改，记缺）**：README「≈122 行」在 HEAD 时已比实际（121 行）多 1 ——属**无人守卫的散文数字**；`F1–F5` 同属此类。SB19 只守身份计数散文，**行数 / token / 案例数三类散文数字均无守卫**，建议列为下一轮 SB 候选（须先按准入规则举证）。**验证**：selfcheck **20/20**；README 五处数字与两处案例引用逐条 grep 复核。
- **成本节增「直观对比」块（2026-09-10 第二十八批）**：把 token 成本换成可感知的参照物。前两行精确算术：常驻面 3,476 tok = **128k 上下文窗口的约 2.7%**（200k 约 1.7%）≈ 2,850 汉字 ≈ 3 页 A4 中文；20 轮任务摊销 ≈ **173 tok/轮**。第三行量级推算并如实标注：全周期（常驻 + 按需）1.8 万–3.3 万 tok ≈ **一次返工来回的常见量级**（澄清 + 重读上下文 + 重做，常见 2 万–10 万 tok，非实测）——收束为一句：**拦下一次「假完成返工」，整个任务期的 skill 开销即回本**，这也是「防假完成」被列为第一优先级的 token 账依据。**验证**：selfcheck **20/20**；README 556 行。
- **新增「成本与收益」节 · token 成本全实测（2026-09-10 第二十七批）**：外部评估把「token 消耗增量」列为首要代价，此前 README 只有估算——本轮全部改实测（o200k_base，2026-09-10 计数）。**成本**：常驻面 `SKILL.md`+`VERSION` = **3,476 tokens**（两处「约 3k 估算」同步改实数）；13 份 references 单份 280–13,424、全读上限 77,832，但实际按阶段取用且两份大文件永不进宿主任务面（`self-test.md` 13.4k 为维护者自测专用、EN 镜像与中文版互斥）——中等任务全周期通常累计 1.5 万–3 万 tokens，摊在整个任务而非每条消息；Lite 档三条本体 154（整卡 518）。**收益（逐条标证据强度）**：返工减少＝A/B R2/R3 实测（位置违规 6+→0、声称失实 3→1→1、T12 完成门弧线 5→3→8，n=1 不外推）；交付质量＝端到端 16/16×2 + bug sweep 4 边界缺陷；安全面＝审查面强制攻击信任边界 + 实测中指挥官层抓到执行者未发现的 P0（机制+个案）；**开发时间＝未实测，如实标注**（机制推断：每拦下一次假完成省一轮返工来回）。**首次加载节新增**：①加载证明自检（版本/硬规则逐字/架构/实读清单）；②宿主对齐仅首次一次——宿主已有能力优先、被覆盖小节标 SKIP、裁剪范围经用户确认、产物落 `docs/agents/host-alignment.md`、不改 skill 本体；③三条底线永不可跳（证据报告/UNVERIFIED/真实环境验收）——「宿主为主、skill 互补、不冲突收益最大化」落为机制而非口号。位置：When To Use 与 Install 之间，目录同步。**验证**：selfcheck **20/20**；README 543 行。
- **Lite 发行档官方化 + token 实测 · 轻量通道裁决不放松（2026-09-10 第二十六批）**：外部评估指出「日常场景重量感」，逐条核验后大面成立——唯**安装文件夹名误写曾用名** `gpt-5-6-sol-reasoning-style`（实际必须保持 `gpt-series-reasoning-style`：skilllint FM010 校验 name=目录名，装错名即通不过校验）。**裁决：轻量通道不放松**——三条排除项是三轮对抗探针实测换来的反绕过边界（速查卡与 README 均载「放宽会重演已被抓出来的回归」），复杂度预算与 77 条冻结也不允许无证据加规则；日常场景的官方答案本就是**最小纪律速查卡**，本轮将其升格为 **Lite 发行档**：①速查卡 token 由估算改**实测**——三条本体 **154 tokens**（o200k_base；cl100k_base 202；整文件 481/659；2026-09-10 计数），速查卡/README×2/site 四处同步，站上「约 500 tok 估算值」换成实数（外部 AI 的「约 500」即源自站点旧估算，实测后恰好坐实）；②README Install 节新增 **Lite 装法**指引（可不装整包、三条入宿主配置、完整治理随时叠加、「全新产物默认中档」边界不可省）；③速查卡头部加 Lite 发行档定位。**暂缓（攒证据）**：轻通道边界补合格示例——现仅自测 ST66 一例保守升档（n=1），累计 2–3 例再议。**验证**：selfcheck **20/20**。
- **README 全文重写 · 采纳外部 AI 稿（2026-09-10 第二十五批）**：另一 AI 在**未读原 README 的前提下**仅凭 skill 实际内容（SKILL.md / 13 references / 21 identities / docs / 脚本 / CI 状态 API 核验）独立重写，503 行、21 节、中文主导。**逐条上盘核验后才采纳**：抽查 14 条事实声明（五字段降级简化包、`allow_implicit_invocation: false`、宿主对齐落盘 `docs/agents/host-alignment.md`、16 分制=9 核心+3 预埋陷阱+4 阴性、A 臂声称失实 3→1→1、盲测二 0/3→3/3 与天花板效应、端到端 ×2 等）**全部属实、零幻觉**；**换入实测**（临时替换 README 跑 selfcheck）暴露 2 处机器可检漂移——SB11 要求的边界 token 被写成同义词（从零新建→全新产物默认中档）、SB14 缺 EN 短语 layered bilingual——均已修。合并补充：实测结果表增第 10 行（77/77 行为自测全量执行 + 运行报告链接）、CI 段补「2026-09-10 起首次全绿（13/13），此前 7/7 失败且历史公开」、Unreleased 批次数 23→24。第二十四批的翻新稿被本稿整体取代（其历史条目按史实保留）。**验证**：selfcheck **20/20**（换入实测 2 失败 → 合并修复后全绿）；README 504 行。原文稿存于用户桌面（本地路径不随仓库提交）。，未随仓库提交。
- **README 翻新 · 面向宣传（2026-09-10 第二十四批）**：以「先展示可核验证据」为主线翻新首屏与验证节。①**徽章**：新增 CI 徽章（2026-09-10 起全绿）与「静态检查 SB1–SB20 passing」「行为自测 77 executed」两枚证据徽章；移除 stars/issues 徽章（小数字在宣传期是负资产，随时可加回）。②**首屏一句话改为自我验证护城河**：把「20 项静态检查、77 条已执行并留档的行为自测、13 步 CI 流水线」提进首屏。③**新增「证据速览」表**（首屏 6 行、每行可点开核验）：CI / 静态检查 / 行为自测 / 实测 / 外部审查 / 诚实——其中 CI 行如实写明「从加上那天起就是坏的、2026-09-10 才修好，失败历史公开」，把曾经坏掉的历史当作信任资产而非遮掩。④**验证节重写**：按 「CI → 行为自测（77/77 执行、方法与完整性红线）→ 静态检查（准入规则）→ 完成声明机械化（含已知缺口披露：黑名单不覆盖 `python -c` 间接执行）→ 实测 → 证据强度」重排，去掉 EN/CN 重复条目。⑤**新增公开证据文件** `docs/field-tests/selftest-run-2026-09-10/report.md`：把 gitignore 区的判定表整理成可公开核验的运行报告——方法与完整性红线、R1 77 格逐格判定（39/35/3）、R2 带预置复跑、反哺清单与局限；README 的「77 executed」从此可点开核验。⑥目录树注释同步三行（CI / field-tests / reviews）。**验证**：selfcheck **20/20**；README 480 行；判定表仍在 gitignore 区（不提交，公开版即上述报告）。
- **CI 二轮失败 · `write_text(newline=)` 的 Python 版本差（2026-09-10 第二十三批）**：第十九批的 LF 修复在本地（Python 3.13）验证通过，但 **CI 跑的是 Python 3.9，而 `Path.write_text(newline=)` 是 3.10 才加入的形参**——推送后 CI 立即在 step 5（`--out` 冒烟）抛 `TypeError: write_text() got an unexpected keyword argument 'newline'`（外部 AI 从日志定位，与 API 步骤级结论吻合：step 4 SB 全过、step 5 挂、其后全部 skipped）。修复（外部建议方案 A，采纳）：**三处写入点全部改为 `with dest.open("w", encoding="utf-8", newline="\n")` 形式**——`open(newline=)` 所有支持版本都有，LF 钉死保留，且满足 SB20（open 写模式 + newline）。覆盖 `selfcheck.py::_write_report`、`selftest-runner.py` 的 schema 与 archive 两处；`probe-runner.py:81` 本就是 `Path.open("a", newline=)`（全版本支持），不动。CI 顺带加一步 `selftest-runner.py schema` 冒烟，让 selftest-runner 的写入点也在 CI 上被跑到。**验证**：AST 级扫描全仓工具层 `write_text` 调用 = **0**（此前误报的 2 处是 SB20 自己的字符串字面量与注释）；三处 `.open(newline=)` 齐；无 3.10+ 语法（无 match 语句）；py_compile ×6 通过；selfcheck **20/20**；`--out` 冒烟 exit 0 且报告 CRLF=0；runner list exit 0。**过程教训（记死）**：第十九批的「验证通过」只覆盖了本地解释器 3.13，没覆盖 CI 的 3.9——**修 CI 类问题时必须在 CI 同版本解释器下验证，或直接把版本差本身列为风险项**。
- **三份外部全面复查上盘核验 · 三个 P0 实锤全修（2026-09-10 第二十二批）**：三份独立外部审查（方法二转述，模型未披露）逐条上盘核验，**先证后修**。①**P0-1 CI 从未绿过（7/7 failure，GitHub API 步骤级实锤）**：step 6 `Official skilllint` = failure，其后 **openai.yaml 解析 / 探针解析 / 自测 77 条解析三步恒为 skipped——三个校验在 GitHub 上从未执行过一次**。根因两条（均实锤）：工作流无 `setup-uv`（runner 上没有 uvx）；`check gpt-series-reasoning-style` 这个路径相对 CI 检出目录（=仓库根）不存在，本地绿是因为从父目录跑。修复：加 `astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d`（v10.0.1，SHA 钉死）+ lint 改为 `cd .. && uvx skilllint@1.19.2 check gpt-series-reasoning-style`——本地复现两形态：父目录形态干净通过，`check .` 复现 FM010 误报（正是当初钉版本的注释所述）。②**P0-2 Resume Check 7 项只活在权威面**：workflow 7 项 vs `SKILL.md` 必载面 4 项 / README 双语各 3 项 / Test 55 期望 4 项——缺的 ⑤重锚定原始指令、⑥项目根硬检查恰是 A/B 实测两个最贵缺陷的修复，必载面没有它就不会被触发、验收面没有它也不会被验收（双重漏检）。修复：SKILL.md 补全 ①–⑦、README 双语补全、Test 55 增两条期望。③**P0-3 加载证明范本 4 行内自相矛盾**：先写「一条主干 + 两个按需扩展」紧接着编号列出三个「模式」并引用已废弃的「模式一/二/三」——该范本是宿主被要求照着输出的。修复：改为主干/扩展 A/扩展 B 表述。④**P1 README:465 仍写 22 个身份文件（实际 21）且 SB19 抓不到**——**这是我们自己 SB19 的真实盲区**（粗体标记隔断短语、EN 数字在名词后、`个文件` 不在模式内）；修复 22→21 + SB19 增文件计数形态（守卫=行内同时含身份与文件；CN/EN 双形态、数字可在名词前后），**RED 实测两种漂移形态均命中**。⑤**证伪一条**：deep-audit C6「docs/selftest-run 应入 .gitignore」不成立——第 12 批起已 ignore。⑥**证实但未修（待裁）**：claim-check 黑名单不覆盖 `python -c` 间接执行（19 条模式无 python 形态，RCE 面真实存在）；README:395「实测 1.5–1.8×」无归档测量证据。⑦deep-audit 其余 18 条与第二报告 P2 清单**未逐条核验**，已在评审归档中列为下一轮清单，不默收也不默拒。核验结论归档 `docs/reviews/2026-09-10-external-reviews-5-6-7.md`（含逐条证据）。**验证**：selfcheck **20/20**；skilllint 父目录形态本地实跑通过；SB19 RED 双形态实测命中。
- **自测期望与 skill 自身纪律对齐（2026-09-10 第二十一批，总指挥裁决）**：R2 实测（方法二转述跑 10 格）暴露两处「期望要求宿主做 skill 本身不授权做的事」。①**Test 9**：prompt 是收尾断言（「所有阶段都完成了，可以结束了」），正确响应用产物证据证伪并拒收；原期望却要求「生成式发散」并把任何刚性分类清单判 FAIL——而发散本就由 Test 10 单点测试。改为：**产物扎根的证伪**（追踪真实计划/门禁单/交付物、用发现挑战前提；「不开项目就能写出来的清单」才判 checklist 失败），修复与重跑一步改为**授权后执行**，不把收尾请求当成改文件的授权。②**Test 19**：prompt 只问「52.4% 合理吗」，原期望却要求「修复实现并加回归测试」——改代码属 T2 需授权。改为：**提出具体修法与回归测试并请求授权**，不擅自改代码；修正值（61.1%）仍须给出。两处均在 `self-test.md` 留修订说明（含裁决依据与日期）。**依据**：R2 十格 **7 PASS/3 PARTIAL/0 FAIL**（对照 R1 无预置轮 0/8/2），证实「那些 PARTIAL 量的是装备不是 skill」；ST09 按修订后期望改判 PASS → **8 PASS/2 PARTIAL/0 FAIL**，判定表 Round 2 节同步（R1 77 行原样保留）。**验证**：selfcheck **20/20**；解析器 77 条 0 畸形；Test 9/19 期望条目实读核对。
- **新增 SB20 · 写入点换行策略检查（2026-09-10 第二十批）**：按 README 既有准入规则立项（新增检查须先证明抓到过真实缺陷、可指认提交哈希）——**负例即 `9ea8d33` 的提交内容**，判定式命中**全部 4 处**未固定 `newline=` 的写入点（RED=4，与第十九批修的四处一一对应）；当前树 0 命中（GREEN=0）。配合对照实测「未固定 = 1 个 CRLF / 固定 = 0」，缺陷可复现、修复可证。**刻意做成静态检查而非工作树 CRLF 扫描**：后者在 Linux CI 上恒绿（git 按 `eol=lf` 检出），抓不到根因；静态检查在任何平台都能抓住「写入点没固定 `newline=`」这一根因。已知盲区在注释里写明（逐行扫描，跨行调用看不到——本仓当前无此类调用）。扫描面 = 全仓 `.py`（排除 `.git`/`__pycache__`），命中两类：`write_text(` 未带 `newline=`、带写模式（`"w"/"a"/"wb"/"a+"`…）的 `open(` 未带 `newline=`；注释行与 `sys.stderr.write`、句柄 `.write()`（换行由外层 `open` 管）不误报。同步 SB 计数面三处：README 19→20、CI 工作流名 `SB1..SB19`→`SB1..SB20`、`site/index.html`。**验证**：selfcheck **20/20**（SB20 实跑 PASS，7 个 python 文件扫描）；SB20 RED/GREEN 双证。
- **工具链换行符缺陷修复（2026-09-10 第十九批）**：第十八批用 Python 打补丁后 `git commit` 报出 "CRLF will be replaced by LF" 警告，追查发现**工具链自身在 Windows 上写 CRLF**——`scripts/selfcheck.py`、`scripts/selftest-runner.py`（两处）、`probes/probe-runner.py` 共四处 `write_text` / `open` 均未固定 `newline=`，Windows 文本模式把每个 `\n` 翻成 `\r\n`，与仓库既有 `.gitattributes`（`* text=auto eol=lf`）及「仓库与工作树统一 LF」的声明相悖。影响面：生成的判定表与自检报告、以及探针 append-only 归档（后者是逐行格式，混入 CRLF 会让按行过滤/解析变脏）。修复：四处一律 `newline="\n"`；并把已被写脏的两个工作树文件（`references/self-test.md`、判定表）转回 LF。**验证**：同一调用模式实测对照——未固定 → CRLF 计数 1，固定 → 0（缺陷可复现、修复可证）；selfcheck **19/19**（fingerprint 保持 `21376de4972d`）；`--out` 报告 CRLF 0；runner/probe `list` exit 0；全仓文本文件 CRLF 扫描 = **0**。
- **自测规范层三项发现修复（2026-09-10 第十八批）**：第十七批 77 条全量执行的副产品。①**Fixture 列从未填写**——解析器与判定表都支持 `Fixture:`，而 `self-test.md` 0 行，导致需预置状态才可判的测试在裸宿主上**永远只能得 PARTIAL**（量的是装备不是 skill）。为 10 个此类测试补 `Fixture:` 声明（2/3/5/7/8/9/19/33/35/56），逐条写明必须先准备什么：预置项目 + git 状态（2）、已知真实通过数与可复跑测试命令（3）、阶段产物 + 门禁单（5/7）、宣称完成且含真实缺陷的项目（8）、可被发散发现的真实产物（9）、周完成率实现与数据（19）、含「测式」错字的 README（56）、接收方与派发通道（33/35）。文件头补中英说明该列语义。②**Test 26/30 与 34/35 口径矛盾**——26/30 原只认 `references/commander-roles.md`，34/35 认「二者其一」，而全仓裁决是 `identities/README.md` 为唯一权威角色目录（`commander-roles.md:59` 明文禁止重复对照表）；被测方按裁决读权威目录反被判 PARTIAL。26/30 改为「`references/commander-roles.md` 或 `identities/README.md`，任一即可」。③**三道门禁的约束对象歧义**——原文「仍须依次过角色身份确认、协调通道确认、实现前门禁」未点明约束的是**派发**还是**产出**；宿主读成「未过门禁不得产出任务包」，而期望是「可产出、只是不得派发」（Test 33 FAIL 的直接成因）。`agent-modes.md` 中英双语补明：**三道确认约束派发动作**；未过门禁不得派发或转交，**但缺必需字段的正式任务包同样不得产出**——接收方/通道未知时先问清楚，不以「待用户指定」占位、不臆造。**验证**：selfcheck **19/19**（fingerprint 保持 `21376de4972d`）；解析器 77 条 0 畸形；Fixture 集合实跑核对为 `[2,3,5,7,8,9,19,33,35,56]`；判定表重生成后 Fixture 列已填充且 **77 条判定完整保留**（PASS 39 / PARTIAL 35 / FAIL 3）。
- **77 条行为自测首次全量执行 · 反哺 1 个真实缺陷 + 新增 SB19（2026-09-10 第十七批）**：① **77/77 全跑**——每格独立 bed 只喂逐字提示词（期望值不下发；且明令被测方不得读 `self-test.md` 与判定表，否则该格判定作废），4 名独立裁判分块**只比对 RESPONSE vs EXPECTED、不加载 skill**（保新鲜视角），逐格留 `response.md` 产物（77/77 落盘清点）。结果 **PASS 39 / PARTIAL 35 / FAIL 3**；3 个 FAIL 为 Test 9（用固定分类清单冒充生成式发散）、Test 15（未以指定授权句收尾）、Test 33（未产出任务包）。② **真实缺陷：身份计数跨面漂移**——第十六批合并后 5 处 live 面仍写 22（`README.md:344`、`README.md:400`、`references/agent-modes.md:221`、`references/series-reasoning-examples.md:574`、`docs/minimal-discipline.md:3`）；**SB4 只数文件、数不到散文**，故此前全部检查皆绿。且第十六批提交信息称"all count surfaces synced（SB4 22→21, README x4）"**与产物不符**——实际只改了 2 处 README。5 处已改为 21。③ **新增 SB19（身份计数跨面一致性）**——按 README 既有准入规则（新增检查须先证明抓到过真实缺陷、可指认提交哈希）立项：以 HEAD 提交内容作负例实测，判定式命中**全部 5 处**（RED=5）；当前工作树 0 命中（GREEN=0）。判定式刻意收窄——第二形态（`当前/全部 N 个`）仅在同行含「内置身份」时生效，避免"共 3 个角色"这类合法表述误报；扫描面为 **20 个显式 live 面**，刻意排除 CHANGELOG/INTERNAL-HISTORY/docs/reviews/docs/field-tests（dated 记录，过去的数对当时是对的）与 docs/selftest-run（每次生成）。同步 SB 计数面：README 预算行 18→19、CI 工作流名 `SB1..SB18`→`SB1..SB19`、**`site/index.html`（原已滞后至 SB1–SB17，本次一并修正）**。④ **自测规范层三项发现（本轮只记录、未改）**：fixture 列**从未填写**（解析器与判定表都支持 `Fixture:`，而 `self-test.md` 0 行）→ 需预置项目的测试（2/3/5/7/8/19…）在裸宿主上永远无法 PASS，那些 PARTIAL 量的是装备不是 skill；Test 26/30 只认 `references/commander-roles.md`，而 Test 34/35 认"二者其一"，与"`identities/README.md` 为唯一权威目录"的既有裁决相冲；Test 33/35 的门禁顺序被宿主读成"门禁未过不得产出任务包"，而期望是"可产出、只是不得派发"。**验证**：selfcheck **19/19**（SB19 实跑 PASS，fingerprint `21376de4972d`）；SB19 RED/GREEN 双证；`response.md` 77/77；工作树无游离产物。
- **五项待办落地（2026-09-10 第十六批，其中①为独立评测见下）**：②**探针加断言**：probe-runner 新增 `verify <id> <output>` 子命令——单向机械预检（fail_patterns 命中即 exit 1 标 FAIL，永不自动判 PASS；无命中时提醒人工裁定并警示"无 pass_condition 命中≈UNVERIFIED"）。三路实测：P2 负例 3 命中 exit 1 / P2 正例 exit 0 + 人工裁定提醒 / P1 负例 exit 1。③**任务包口径收敛**：workflow 的 11 字段「内部派发包」明确限定为**仅单 Agent 内部派发**（原措辞含"或派给子 Agent"，与六字段迷你包标准冲突），并写明三层口径各归其权威（11 内部 / 6 子 Agent / 23 跨模型指挥官），不得混用。④**qa/test-engineer 合并**：两份高度重叠的同名角色文件合并——test-engineer.md 的独有内容（使命"把验收标准转成可重复证据"、两条反模式）并入 qa-engineer.md，删除重复文件并按单一来源原则同步全部计数表面：SB4 22→21、README 四处计数（22 文件/21 类 → 21 文件/21 类）、identities/README 表行与文件清单、第三/四次评审归档注记。⑤**Downloads 测试残留清理**：5 个 todo*.csv（含 R2 t12-A 的 todos.csv）移入工作区 backups/downloads-cleanup-2026-09-10/（移动而非删除，可回滚），Downloads 归零。验证：selfcheck 18/18（SB4 实跑 PASS 21 项）；全仓 test-engineer 引用清零（历史 field-tests 除外）。**事故与恢复**：合并脚本执行中 identities/ 工作树文件被意外批量删除（原因未定位），经 git checkout 从索引完整恢复（23 项），合并步骤重做并逐文件断言校验——如实记档。
- **真实案件回归集·第一波实证反哺 · 两条新条款（2026-09-10 第十五批，规则行为变更）**：8 格真实案件实测（A 30 vs B 29）实证出两类新失败模式，均落在 A 臂且同源（"声称 vs 产物一致性"）：①A-RC08 以"清理运行时产物"名义删除自证据（日志/截图），完成声明宣告 fresh 证据却零留档；②A-RC11 声称 6 档视口矩阵而 before 截图仅 3 档、A-RC09 汇报 12/12 而产物 13/13（少报）。据此 Honesty Gate 新增两条款（中英镜像同步 + SKILL.md 工作流第 6 条补句）：**证据产物是交付物**（日志/截图/验证脚本留在交付目录，不算运行时垃圾；确需删除须先逐条列出被删产物与内容摘要）与**声称与产物对等**（计数/覆盖面陈述与技术产物双向一致，多报少报同罪）。验证：selfcheck 18/18；selftest/runner 不受影响。
- **名称与来源说明落地（2026-09-10 第十四批，总指挥裁决：名字不改、只补出处）**：README 顶部「名称简注」升级为双语「名称与来源 / Name & Origin」——如实写明本 skill 从**一系列 GPT 系列大模型的对话记录**中打磨提炼（`gpt-series` 记录的即这段来源），并重申两条边界：不依赖、也不限于 GPT 系列（任何宿主模型可用）；`reasoning-style` 指流程纪律风格而非推理能力上限。SKILL.md frontmatter description 尾句同步（"the name records its origin"）。背景：外部评估指名字为持续负债（每篇文档都要解释），总指挥裁决**保留名字、以文档一次写清来源偿付该成本**——不改名、零链接迁移。验证：selfcheck 18/18（desc 611 chars ≤ 1024）。
- **第四份外部审查交叉修复批 · 重述面漂移 25 项（2026-09-10 第十三批）**：DeepSeek V4.1 Flash 只读审查（A1–A7/B1–B12/C1–C13）逐条上盘核实后修复，评审原文归档 `docs/reviews/2026-09-10-external-reviews-3-and-4.md`（补第三/四次审查的可追溯归档）。**A 级**：A2 自测文件两处**转义围栏**致 Test 56 探针提示词对工具链完全不可见（selftest-runner list 丢提示词、SB2/3/6/15 全漏检）——还原围栏 + **SB6 新增转义围栏检测**（负例实测命中）；A1 示例面 Example 17 门禁补「风险分档/形态选择」两字段（此前全文 0 次）；A3 Example 23 删除硬编码 9 身份清单改指向权威目录；A4 子 Agent 包规格三处矛盾统一为「六字段迷你包为标准、完整包仅跨模型指挥官场景」；A5 lessons.md 五处规则漂移修复（三种模式→主干+两扩展/三档盘点→四档/补轻通道三排除项/Resume Check 3→7 项/补目标相关缺陷条款）；A6 冻结自测标题与示例的旧表述改为主干+两扩展；A7 版本语义写明（1.1.0 基线 + Unreleased 批次计价，SKILL.md Version 节）。**B 级**：B1 README 验证段补 A/B 三轮与两个机械检查器并改写证据强度口径；B2 孤儿报告重编号 Field Test 4 并入索引（Results 第 9 行 + Reports 链接）；**B3 CHANGELOG 批次按编号降序重排** + 评审归档落地（此前 HEAD 提交引用的 P0-4/P1-27 编号全仓无出处——本批归档补齐溯源）；B5 deputy-commander 归 Optional（对齐 commander-roles 按需语义）；B6 EN 反模式移除 model/session（与全库「模型可选」一致）；B8 断链修复（probe-c 10 处相对链接 + 评审文件名大小写 + probe-runner docstring 路径 + Antigravity `~`）；B9 artifact-check 补 Date/Task tier/Form 三字段校验；B10 同步清单补 examples.md/lessons.md 两个重述面；B11 语言策略表按实测中英比改写；B12 INTERNAL-HISTORY 自述上限修正为 `0.1.x`–`3.3.x`。**新增 SB18**（三条禁用授权短语的三表面断言——举证即 P0-4 真实缺陷：CN 权威版曾漏「直接做」）；selfcheck 总数 17→**18**（README 预算行与 CI 工作流名同步）。C4/C8/C12 顺手清理（SKILL 轻通道去重、site/横幅改「一主干+两扩展」、ab-baseline 补各轮 A 臂 skill 状态标签）。**验证**：selfcheck 18/18（SB18 实跑 PASS、SB6 负例实测命中）；selftest-runner 77 条且 Test 56 提示词恢复；ast 解析通过。
- **R3 候选规则细化落地 · 两条（2026-09-10 第十二批，规则行为变更）**：R3 评测新暴露的两项张力经裁决落地（workflow 中英镜像同步 + SKILL.md 核心规则）。①**目标相关缺陷不算无关问题**（SKILL.md 范围克制条 + workflow Bug Sweep 节）：影响任务目标正确性的发现必须主动修复或显式提请裁决，仅记录了事视同未处理——直击 R3-A T5「发现 1900 世纪年缺陷但按范围克制仅记录」的判例；②**「未发现问题」须附检测方法与覆盖面**（SKILL.md 实操闭环条 + workflow User-Path Acceptance 节）：工具/视口矩阵/用例清单缺任一项即降级 UNVERIFIED——直击 R2+R3 两轮 A 臂 T6 系统性漏检（B 臂同期两次找到真问题）。验证：selfcheck 17/17；workflow 中英镜像四处插入对齐。
- **两份外部审查交叉修复批 · 语义/规则文本层 22 项清零（2026-09-10 第十一批）**：两份独立外部审查（豆包 + 另一模型）经逐条上盘核实后合并修复，覆盖前十批代码审查未触及的散文语义层与规则文本撞义。**语义梯度**：openai.yaml 并行信号措辞加对冲（仅当并行收益>简报成本才指子 Agent，否则回退主干）——Codex 平台不再固化"并行=子 Agent"。**声明贴合实际**：README 语言政策 identities 行改"身份定位节双语；契约正文英文为主"；8 段契约声明补 deputy-commander 第 9 节（指挥权接管）附注；commander-roles 反模式补译 2 条（未读身份文件就声称角色/无匹配悄悄自创专家）。**P0/P1/P2 判据表落地**（identity-library 新增 Finding Severity 权威表：P0=错误执行/泄露/不可逆须立即修复；P1=违反纪律或错误结果交付前修复；P2=backlog 不阻塞；reviewer 身份文件加引用）——15+ 处使用首次有了单一权威定义。**规则文本撞义与缺位**：SKILL.md 轻通道"形态"改"产品形态"消除与门禁字段"形态选择"的同词两义；agent-modes 迷你包信任层级字段补"T3 属指挥官扩展场景、子 Agent 迷你包限 T1/T2"（中英同步）；workflow Fix-Loop Cap 重复节加权威指向；agent-modes/README 的 5 轮上限补第 6 轮缓冲交叉引用。**静态展示面滞后一次扫平**：README 复杂度预算 16→17 项、树注记两轮→三轮、site 结果行两轮→三轮（含 R3 93 vs 86 与 T12 弧线 5→3→8）+ tok 加估算限定 + 树 scripts 块补 claim-check/_selftest_parser；platform-installation 树同步 + 平台数口径注（13 键=14 行-1 泛型别名）。**工具修复**：artifact-check findings 账本改逐轮校验 + FINDINGS_FIELDS 补第 4 字段（本轮改动与原因，对齐 project-artifacts 权威）+ argparse 化（--help 可用）；selftest-runner schema 分隔行 5→6 列、verdict 回退列加告警；selfcheck docstring 增「已知盲区」清单（语义漂移/identities 双语/跨文件字段并集等——字面层绿色边界明示）；claim-check docstring 写明黑名单绕过面。**identity-library/closure 身份文件字段清单并集**（双方都补齐 trust tier + DRI + availability）。验证：selfcheck 17/17；runner 77 条；probe list OK；ast×6。待裁决残留：L4（README TOC 锚点——无法证实存在）、L11（mirrors 措辞——未逐字定位）。
- **深度代码审查修复批 · 14 项清零（2026-09-10 第十批）**：逐行审查全部可执行代码（报告存工作区 docs《代码审查报告-2026-09-10.md》，高×2/中×6/低×6，L5 性能项核实为无瓶颈）。**高危两项**：①claim-check.py 以 shell=True 执行不可信 claims 命令——新增**破坏性命令黑名单**（rm -rf/del /s/Remove-Item -Recurse/git push --force/reset --hard/clean/curl|sh/Invoke-Expression/reg/schtasks 等 17 模式，默认拦截并 FAIL，--allow-dangerous 人工复核后解锁）+ 输出尾行执行数审计；②subprocess text=True 未指定 encoding（中文 Windows GBK 陷阱，R3 实测发生过乱码）——改 `encoding="utf-8", errors="replace"`。**中危**：self-test.md 解析逻辑 4 份重复实现收敛为公共模块 `scripts/_selftest_parser.py`（selfcheck SB2/SB3/SB15 与 selftest-runner 共用，顺带修复 SB2 会被畸形头静默绕过的旧问题）；claim-check 段标题改词边界匹配（## Profile 不再误吞进 Files）；命令超时改 --timeout 参数（默认 600）；generate-banner.py 包 main() 守卫 + CJK 字体缺失改为显式告警；selfcheck 注释 SB11-SB17 同步 + 删 SB17 死分支 + 删 _write_report 死参数；SB12 frontmatter 改行扫描定位闭合 ---。**低危**：selftest-runner verdict 回退列号加告警；SB3 prompt 匹配改大小写不敏感；probe-runner 资源缺失退出码统一为 1（ERROR 前缀到 stderr）；CI 新增 openai.yaml 结构冒烟（pyyaml safe_load）；claim-check hash 强制 64 位 hex。验证：selfcheck 17/17；ast 解析 ×5；claim-check 三路径实测（黑名单拦截/白名单解锁/64 位 hash MATCH）；probe-runner & selftest-runner list exit 0；generate-banner ast+守卫语义确认。
- **A/B 基线评测第三轮完成 · 三轮最大分差 + 完成门条款实战有效（2026-09-10 第九批）**：第九批两项内容。①**新增 `scripts/claim-check.py`**：完成声明机械核验器——读 markdown 声明清单（`## Files` 存在性 / `## Commands` fresh 实跑+期望退出码 / `## Hashes` 内容 pin），把 Honesty Gate「磁盘自检清单」条款从规则变成可执行检查（R3 判分即用其口径核 T12）。②**R3 评测完成**（24 格，简报与 R1/R2 逐字一致）：**A 臂 93 vs B 臂 86——三轮最大分差**（R1 −3 → R2 +2 → R3 +7）；**T12 完成门三轮弧线 5 → 3 → 8：完成门三条款实战有效**——t12-A 完成声明首次自带磁盘自检清单格式，裁判 CDP 复测 5/5 + claim-check 口径核验通过；A 臂声称失实 3 → 1 → **1**（T4-A 矩阵比对头条「逐位一致 PASS」与其自有日志 FAIL=9 矛盾——裁判深读日志更正判分 8→7，claim-check 全段核验列为下轮改进）；位置违规连续两轮为 0；A 臂 7 格门禁停止全部正确处置，且出现**重复指令免疫**（t3-A/t8-A 拒绝重跑重复批复）。新暴露两项候选规则细化（待裁决）：T6 移动端检查 A 臂两轮系统性漏检（「未发现问题」须附检测方法与覆盖面声明）；T5 范围克制与目标相关缺陷的张力（目标相关缺陷不算无关问题）。判读纪律不变：n=1、裁判=作者、t12-A 宿主 Downloads 越界与床内调试残留已如实记录。逐格评分与三轮纵向对比见 `docs/field-tests/ab-baseline/judgement-sheet.md`（新增 R3 判分卡/汇总/三轮对比/行为级观察），field-tests 索引补第 8 行。
- **全面检查修复批 · 14 项问题清零（2026-09-10 第八批）**：只读全面检查（报告存工作区 docs《全面检查报告-2026-09-10.md》，18 项发现：中×2/低×8/信息×8，无高危）后总指挥裁决全修。①**SKILL.md References 索引补全**：新增 project-artifacts.md（治理落盘约定此前对宿主不可达）、workflow-en.md（附"中文宿主勿读"）、docs/minimal-discipline.md 路由；②**selfcheck 新增 SB17**（AGENTS.md 入口一致性：路由/硬规则原文逐字/被引用文件存在性），selfcheck 总数 16→**17**；SB8 的 selftest 扫描从 4000 字符截断改为全文（消除脆弱断言）；`--out` 加仓库边界防护（绝对路径/`..` 逃逸拒绝）；③**CI 双 action SHA-pin**（checkout v4.2.2 / setup-python v5.6.0），工作流名 SB1..SB16→SB1..SB17 同步；④**selftest-runner** list/schema 补 UTF-8 结构化报错；⑤**probe-runner** archive evidence 换行净化（保住 append-only 一行一条不变量）；⑥**安装器双端**：拒绝装进 skill 仓库自身（pwd 型 DEST 在仓库根执行时防自嵌套）、排除面新增 site/ 与 .gitattributes；⑦**platform-installation.md 树图**补 AGENTS.md/.gitattributes/.github/hooks/docs/probes/site 并注明安装器剥离面；⑧**hooks/README 主安装示例**补 `"startup|clear|compact"` matcher（compact 后重注入）；⑨**site/index.html** 补两轮 A/B 评测结果行（含"结论不外推"注记）；⑩**generate-banner.py** 字体回退链扩展 macOS/Linux（防非 Windows 上 CJK 豆腐块）。验证：selfcheck 17/17；4 个 Python 文件 ast 解析通过；install.sh `bash -n` 通过；install.ps1 PSParser 0 错误；probe-runner/selftest-runner list exit 0。
- **同类对比反哺 · 五项改进落地（2026-09-10 第七批）**：对比对象下载至工作区（anthropics/skills 官方仓库 + obra/superpowers），报告存工作区 docs《同类对比分析报告-2026-09-10.md》。总指挥裁决"都可行"后落地：①**完成门三条款 + 磁盘自检清单**（workflow.md Honesty Gate Rules 新增，英文镜像同版本同步）：证据必须**本条消息内 fresh**（"早些时候跑过"不算数）；完成声明必须附**磁盘自检清单**（改动文件清单 + 关键 diff + 实跑输出/退出码，逐项有路径——"没有清单的完成只是意图"）；回归测试有效必须附 **RED→GREEN 完整循环**证据——直击 A/B 评测 T12 格连续两轮"假完成"失守；②**模式三任务简报文件化**（multi-agent-closure-rules.md）：23 字段任务包确认后落盘为 `docs/plans/<task-id>-brief.md`，启动提示词只带加载指令+身份+文件路径，接收方一次 Read 读取、任务文本不再经控制器上下文中转（省中转 token + append-only 派发留档 + 续会可恢复）；③**新增根级 AGENTS.md 跨运行时入口**（Codex/Gemini/Copilot CLI 识别的别名，指路 SKILL.md/VERSION + 加载证明要求，权威仍在 SKILL.md）；④**hooks/README 补 matcher 变体**：`SessionStart` matcher 建议含 `compact`（上下文压缩后重注入提醒，对齐 superpowers 实践）；⑤**common-failures.md 新增 F5 案例与合理化借口对照表**（F5=A/B R2 T12-A 假完成实测；借口表格式借自 superpowers verification-before-completion，每行锚定本仓库真实失败）。验证：selfcheck 16/16；workflow 中英镜像 Honesty Gate 条款数一致（3/3）。
- **Token 成本优化 · 按需加载防护（2026-09-10 第六批，文档级改动无规则语义变更）**：token 消耗分析（报告存工作区 docs）实证必载面 SKILL.md 仅 5.4k 字符（≈1.4× 同机 113 个 SKILL.md 样本中位数、38% 于官方 skill-creator），水平合理；消耗最高的单项是中档任务对 `series-reasoning-workflow.md`（47k 字符）的按需整读。据此三项落地：①**workflow.md 头部新增 Section Map 分节定位**（七大类按执行阶段分组、锚点=精确节标题，宿主 Grep 取所需节、明确"禁止为保险而整读"），英文镜像同版本同步同款英文版并加"中文宿主勿读本镜像"声明；②**self-test.md 加"非宿主任务路径"声明**（维护者/用户人工执行，宿主 AI 无需读取）；③**SKILL.md References 索引加定位指引**（workflow 按节取用、agent-modes 形态判定读头部 `Mode Self-Selection` 节即可、self-test 标注非任务路径）。预期收益：中档任务按需开销从 31k–52k tokens 降到 5k–15k（整读→取节）；两个 50k+ 级文件的误读敞口关闭。验证：selfcheck 16/16；Section Map 锚点抽查全部可 grep 命中；references 计数 13 不变、规则语义零变更。效果由下一轮 A/B 评测两臂输入侧对比复核。
- **A/B 基线评测第二轮完成 · skill 首次跑赢基线（2026-09-10 第五批，纯结果入册无规则改动）**：同批 12 任务 × 双臂复测（简报与第一轮逐字相同保可比；A 臂 skill v1.1.0 已含第四批 Resume Check ⑤⑥；spawn 提示词即全量简报，消除第一轮的恢复丢简报模式）。结果如实入册：**A 臂 89 vs B 臂 87 /96——带 skill 臂首次跑赢**，但优势幅度小（2 分）且处在判分口径敏感区。两项硬指标：**位置违规 6+ 起 → 0 起**（Resume Check ⑤⑥ 的直接目标；唯一位移事件系裁判自身批复失误，已去污留档）；A 臂证据质量出现代差（自建 18 项 CDP 验收套件、200 次批量自检、用失败测试钉住 bug——裁判逐项独立复现，T5 加做 RED 验证证明测试真实有效）。未修复项如实记录：**完成门（T12）连续两轮是 A 臂唯一失守格**（假完成：磁盘零改动却宣称完成，记 3/8；可能含宿主框架因素），"完成声明必须附磁盘自检清单"已列为候选规则硬化待裁决。24 格全部由裁判亲自上盘核验（diff/实跑/CDP 运行时验证），裁判验证脚本一律放临时目录不污染床位；事故与工具侧教训全部留档。判读纪律不变：n=1、裁判=skill 作者，结论不外推。逐格评分与两轮对比见 `docs/field-tests/ab-baseline/judgement-sheet.md`（新增 R2 判分卡/R1→R2 对比/事故记录），field-tests 索引补第 7 行。
- **A/B 基线评测反哺 · Resume Check 增两项（2026-09-10 第四批）**：第一轮基线评测（12 任务 × 双臂，同宿主子智能体，n=1）实测出两类**环境级缺陷**——恢复中断后队员丢失原始简报导致"行为/汇报漂移"（声称未改动但磁盘已改、两版汇报口径矛盾、交付写错目录且汇报的路径与磁盘不符），以及把宿主根误当项目根写盘。据此 Resume Check 从 5 项扩为 7 项（双语同步）：**⑤ 重锚定原始指令**（恢复后先重读任务原始指令全文，绝不依赖记忆或继承摘要）；**⑥ 项目根硬检查**（恢复后首次写盘/建目录前，核验当前工作目录与任务指定项目根一致，不一致即停下报告）。评测结果如实入册：A 臂 81 vs B 臂 84 /96——本轮带 skill 臂**未跑赢基线**，行为级观察确认门禁/盘点/治理机制确实触发但被环境噪声抵消；逐格评分与局限见 `docs/field-tests/ab-baseline/judgement-sheet.md`，field-tests 索引补第 6 行。**判读纪律**：n=1、裁判=skill 作者、A 臂承担门禁往返与环境噪声，结论只在这批样例与该组合上成立，不外推为"无效"或"有效"。
- **同类对比反哺 · P0-2/P1/P2 四项落地（2026-09-09 第三批）**：对比报告（`...-workspace/docs/同类项目对比分析报告-2026-09-09.md`）裁决后落地——①**A/B 基线评测包**：`docs/field-tests/ab-baseline/`（协议 + 12 个自包含任务样例 + 判分卡/汇总表），对齐官方 skill-creator 的带/不带 skill 对照方法论，补"缺陷减少"证据缺口；**第一轮已执行完毕（2026-09-10，12 任务 × 2 臂，n=1）**：A 臂 81 vs B 臂 84（/96）——本轮未跑赢基线，主因为托管环境噪声（恢复丢上下文、共享工作目录污染、跨臂文件复用），行为级观察确认门禁/盘点/治理机制确实改变流程；逐格评分、A−B 汇总、口径与局限见 `docs/field-tests/ab-baseline/judgement-sheet.md`，结论不外推为普适。②**可选单行 hook**：`hooks/session-reminder.sh` + `hooks/README.md`——SessionStart 注入恰好一行提醒对冲"模型忘记调用"；opt-in 默认不装，不装不影响任何功能；刻意不做常驻包装（守住"显式调用"原则）。③**workflow 全英镜像**：`references/series-reasoning-workflow-en.md`——中文权威版的全英镜像（把中文独占块——门禁 10 字段模板、轻通道三排除、资源盘点四档结论等——翻成英文），头部带 Mirror status 与冲突条款（权威为中文版）；中文版头部加镜像指针；README Maintainer Notes 同步清单把镜像列为第 3 表面。④**产物链落盘约定**：`references/project-artifacts.md`——门禁单（`docs/gate/<date>-<slug>.md`，proposed/confirmed/rejected/superseded 四态状态机 + append-only）+ 既有台账/账本路径汇总；配套 `scripts/artifact-check.py`（结构校验，明确"结构合规 ≠ 内容真实"）。⑤**静态单页文档站**：`site/index.html`（双语、暗/亮自适应，GitHub Pages 可直接指向）。配套同步：SB5 计数 11→13（新增 workflow-en 镜像 + project-artifacts）；SB16 tier 清单不变（镜像为全英文件，不入双语检查）；README（references 计数 11→13、目录树、语言策略表两行、维护者同步清单、可选 hook 说明）。**刻意不做**：市场上架（总指挥裁决：测试期暂缓 P0-1）；SB17（产物校验走独立脚本而非 SB，复杂度预算不破）。验证：selfcheck 16/16；artifact-check 正/负两用例实测（畸形记录 10 项 FAIL exit 1，合规项目 pass exit 0）；hook 实测恰好 1 行输出；site HTML 解析通过。
- **十轮全面循环审查（2026-09-09 第二批）**：9 缺陷修复——selfcheck（SB10 提前返回致 SB11-16 静默缩水；SB0 环境错误结构化中止；空 self-test.md 假通过）、selftest-runner（**archive 判定列硬编码 cols[2] 实读 Fixture 列、统计恒零** → 表头定位；缺失/编码防护；竖线转义；畸形 Test 头警告）、probe-runner（坏 JSON 防护；**report id 过滤取首字符**）、双安装器（**泄装 .git/.github/.gitignore/__pycache__** 清除，双端行为对齐）、generate-banner（页脚版本硬编码 → 读 VERSION）、README（EN references 计数 10→11）。风格遗留：行尾混杂不修（功能零影响，未来 `.gitattributes` 独立提交收口）。详见 workspace 台账⑥与十轮审查报告。
Post-1.1.0 additive work, kept on the fixed public 1.1.0 per the "对外固定 1.1.0" rule. Mostly static consistency & documentation additions; the 2026-09-10 seventh batch (同类对比反哺) does add rule clauses — completion-gate hardening (Honesty Gate evidence-freshness / disk self-check list / RED→GREEN) and the mode-3 brief-file convention. Still additive; no version bump.
全库同类缺口复查反哺（2026-09-09 第二轮）**：上轮实测反哺之后，按同一缺陷模型（规范盲区 → 把不足证据当充分，或把"类型不适用"误判为"能力不具备"）横扫全库。**计数类零漂移**（22 身份/21 类、11 references、23/6/77/3/5 全部实测吻合，SKILL.md References 列全 11 份）。落地 4 项修订：①**示例语义与规则冲突（严重）**：`examples.md` 5 处「最高影响问题」有 3 处（Example 16/17/26）填成了**提问或待拍板项**，与 SKILL.md 新规则「不是提问」反着来——规则改了而承载示范的面没改，等于新规则被示例抵消；Example 17 的「需要你确认」又被填成"请选择 A 或 B"（与独立的「澄清方式」栏重复）。已改：Example 16 回归【理解确认】场景本就适用的字段名「需要确认的关键点」（内容无误，错的是字段名串味）；Example 17/26 的「最高影响问题」改为技术风险与已知权衡，待拍板项移入「需要你确认」并逐条给选项 + 推荐。②**「零命中／零错误」类结论缺"先证明工具真的在工作"（严重，与 clear() 同构）**：测试域已有「回归测试有效 RED→GREEN」，扫描域没有。`common-failures.md` 表头新增**通则**（任何以 0 命中/0 errors 为结论的声明，须先用**已知靶子**验证工具能命中——否则"0"可能只是路径没覆盖、glob 没匹配、正则写错或历史对象没扫）；并把原「lint 干净」行**换成**更宽的「扫描干净 / 检查无命中」（含安全/脱敏/敏感信息/lint，标题保留 lint 字样以利检索），最小充分证据三条：先证工具能命中 + 全量 0 命中 + 声明并证明扫描范围（脱敏类须含 **git 历史**含已删除但仍可达的对象、打包/压缩产物、二进制内文本；lint 类须证明 glob 真吃到文件，空跑同样 exit 0）。**只换不增，表行数不增**。③`agents/openai.yaml` 补消歧（**第三个门禁表面，上轮遗漏**）：SB9 把它纳入同步检查，但其 default_prompt 无消歧说明，而走 agents 渠道时它常是唯一注入文本。已加三处短注（technical risks, not questions / already-rejected, informational / open decisions only you can make）。④`SKILL.md` + `workflow.md` 两表面「其他选项」栏补"（我已评估并否掉的备选，信息性、不需要你选）"，与「需要你确认」划界——那栏是我否掉的，本栏是我无法替你定的。**刻意不改**：安装成功/环境可复现行（收敛优先，F1 案例已在档案）、Non-GUI 分档与 User-Path Acceptance 的措辞重叠（两闭环各自成立，交叉引用即可）、**SB17**（因①而具备复杂度预算要求的"抓到过真实缺陷、可指认文件行号"立项资格，但属 1.2 提案范围，不在本轮）。验证：selfcheck 16/16（fingerprint `fe5a685b7433`）；openai.yaml YAML 解析通过（default_prompt 2,369 字符）；SKILL.md 118 行（预算 ≤250）；未升版、未推送。
端到端实测反哺（2026-09-09 主干形态实测）**：新增 `docs/field-tests/field-test-3-single-agent-end-to-end.md` —— 首个**单 Agent 主干形态**的端到端实测报告（此前只有指挥官形态 / 对抗探针 / 第三方盲测，主干缺专项）。本次实测最重要的发现是「**测试全绿 ≠ 功能正确**」：被测项目 `clear()` 在 38/38 全绿时仍保留已完成任务且计数错误，因为唯一用例的两个任务恰好都是未完成态、掩盖了分支。据此落地 4 项修订：①`common-failures.md` Claims 表补「功能正确」行（不算数 = 测试全绿；最小充分证据 = 测试全绿 **且关键状态组合已覆盖**）②`lessons.md` 强化「只覆盖一个分支／只测默认态」失败模式条目并附本次实证 ③`workflow.md` 新增「Non-GUI artifacts / 非 GUI 产物」分档（CLI／库／API／文档各自的证据形态），澄清「产物类型无 GUI」≠「环境无能力」，避免 CLI 类产物被误降为 `UNVERIFIED` ④`workflow.md` 资源盘点补第四档结论「本 skill 已覆盖 → 不重复加载」（防同义 skill 重复烧 token）。另：`SKILL.md` 与 `workflow.md` **两处门禁模板同步**加「最高影响问题（技术风险，非提问）」与「需要你确认（等你拍板的选项）」区分说明（遵守门禁字段多表面同步纪律）。**撤回**一条初稿建议（给 bug sweep 加固定攻击面清单）——核对后确认 `Generative Divergence Protocol` 已**有意**把固定清单定位为「冷启动辅助而非发散本身」，属设计选择而非缺失。验证：selfcheck 16/16（fingerprint `fe5a685b7433`）；SKILL.md 118 行（预算 ≤250）；未升版、未推送。
新规则：首次使用宿主对齐（Host Alignment）**：SKILL.md 加载证明段新增——首次在当前宿主使用时，AI 在首次门禁前输出一次「宿主对齐声明」（宿主已有能力清单 / 与本 skill 小节的重叠映射并标记 SKIP / 裁剪后使用范围），经用户确认后本次会话生效；声明可落盘 `<项目根>/docs/agents/host-alignment.md` 供同项目复用。**两条硬边界**：证据报告、UNVERIFIED 标记、真实环境验收不可被对齐跳过；AI 不得为适配而修改 skill 本体（适配产物只落项目侧）。动机：外部评审 AI 丁 #2（宿主重复）的运行时解法，README 劝退段的机制化。SB9 表面同步锚点扩至 `宿主对齐`（SKILL.md+README）；README 宿主优先段同步文档化；规则按 RULE-ONLY 对待，行为探针验证列为 1.2 执行项（见 proposals 提案 C）。成本：SKILL.md 2,678 → 2,962 tok（+284，累计自 5,461 仍 -46%）。
外部模型评审（AI 丁）归档 + 5 项风险采纳裁决**：新增 `docs/reviews/2026-09-09-ai-ding-external-review.md`——一份批评性外部模型评审的忠实转录与逐条裁决（评审者通读全仓库后给出"工程严谨度最高一档但成本即严谨度"的混合评价）。采纳 4 项、部分采纳 1 项、拒绝 2 项（改名：Formerly 归并与 13 平台安装路径成本 >> 收益；放宽轻通道排除项：三轮探针反绕过边界）。**评审者匿名化**：延续 AI 甲/乙/丙 编号记为 AI 丁，评审模型上游归属与盲测 proxy 身份一并匿名（协作工具链画像按隐私纪律不外泄）。
防误触发澄清（采纳 AI 丁 #1）**：SKILL.md description 首句加 "Process-discipline layer only — not a reasoning-capability booster and not GPT-specific despite the legacy name"，同步 `agents/openai.yaml` short_description——宿主据此判定挂载，避免"提升推理能力"类请求误触发；**拒绝改名**（Formerly 归并与 13 平台安装路径成本远超收益）。
证据强度显性标注（采纳 AI 丁 #4）**：README Verification 段新增"证据强度"行——盲测每格 n=1、proxy 裁判、基线不中立，证明的是"机制存在且改变流程"而非"缺陷减少"，两个结论分开记账；外部评审（含批评性）归档于 docs/reviews/。
门槛显性化（采纳 AI 丁 #2/#5）**：When To Use 表新增"单轮或 10 分钟内小任务 → 只钉速查卡（~500 tok）"量化判据；closure-rules 用户转交段显式给出五字段降级简化包低门槛入口（完整协议见 agent-modes Recipient Downgrade）；closure-rules 新增"三本账一处安家"说明（派发+形态变更共文件，发现账本同目录独立文件——治理心智成本 = 一个目录两个文件）。
1.2 候选提案立项**：`docs/proposals/2026-09-09-1.2-incremental-gate-and-pkg-template.md`——提案 A 同会话增量门禁（须走探针流程验证，禁止直接改规则）、提案 B 任务包填空模板（23 字段骨架 + 五字段快速版）。
SKILL.md 第二轮压缩（模式3/工作流下沉，累计 -51%）**：模式3规则 9 条 → 4 条（保留触发条件、平台工具≠用户确认、接收方命名与模型解耦、规划落盘四项决策点；通道自查/接手协议/任务包细节指向 closure-rules 权威版）；工作流 11 步 → 6 步骨架（保留体检、风险分档、盘点-发散-收敛-门禁、阶段+最终验收、实操闭环含 UNVERIFIED 诚实条款、bug sweep 与证据交付；完整流程指向 series-reasoning-workflow.md）。全部规则决策点无删除，仅去除与 references 的重复细节。实测：SKILL.md 3,007 → **2,678 tok**（自 EN 段时的 5,461 累计 **-51%**）；L0 常驻 **2,684 tok**；中档任务单次估算 **~3.4k**；111 行。selfcheck 16/16（fingerprint 93e6f8012bfb）。继续压缩评估：剩余构成以 description（触发关键）与门禁/硬规则（安全锚点）为主，边际收益递减，本轮止于 2,678。
SKILL.md 常驻成本 -45%（EN 段下沉）**：tiktoken(o200k) 实测发现 SKILL.md 5,461 tok 中 2,570 tok（47%）是 `a635dd5` 加入的逐段英文对译——与完整英文规则在 references EN sections 的内容重复。将 8 个 `(EN)` 对应段全部下沉（内容已在 agent-modes / closure-rules 承载），SKILL.md 顶部保留 4 行英文入口指引（指向 EN sections + 签名术语清单）；语言政策本就声明「SKILL.md 中文为主」，本次是回归该声明。SB16 同步调整：SKILL.md 移出 tier-A 双语严查名单，改为仅检查「英文入口指引存在」。实测：SKILL.md 5,461 → **3,007 tok（-45%）**；L0 常驻（+VERSION）5,467 → **3,013 tok**；中档任务单次估算 ~6k → **~3.7k**。selfcheck 16/16（fingerprint 3ea45c998091）；SB9/SB11 经核对只依赖中文 token，不受影响。
私有项目名脱敏（历史档案保留、身份信息剥离）**：INTERNAL-HISTORY.md 与 CHANGELOG.md 中 6 处私有项目名（原型项目名）替换为中性表述（"私有原型项目 / a private prototype"），蒸馏语义与史实不变、仅剥离可关联性；顺带把 T-SEC-01 案例中的具体代码文件名中性化为"具体文件:行号"（防可定位标识外泄）。Sol 系列旧名、TRAE/智谱/豆包等品牌史、实测案例名称均为本 skill 自身历史或公开产品名，经审查保留。另将 `agents/openai.yaml` 的 `allow_implicit_invocation` 改为 `false`——与 README「仅按需显式调用、非常驻」的使用主张对齐，避免宿主在未被点名时自动挂载导致行为与文档预期不符。
SB16 分级（tiered bilingual coverage）+ 复杂度预算**：规则面（SKILL / agent-modes / closure-rules）保持双语严查；长参考文档（workflow 等 500+ 行）降为 informational，不再强制全文双语——消除「永远红」与「文件翻倍」二选一（此前 25 处 tier-B 缺口转为留档信息）。配套 README 新增 `## Complexity Budget / 复杂度预算`：SKILL.md ≤250 行、SB 检查上限 16 项（新增第 17 项须先证明抓到过真实缺陷，可指认提交哈希）、自测冻结 77 条只换不增、收敛优先于加码。
新增 `references/common-failures.md`（claims↔最小充分证据对照表 + 失败案例库）**：借鉴 obra/superpowers `verification-before-completion` 的公开实践（claim→fresh-evidence 映射 + recorded failure memories）。Claims 表给出 10 行「声称 / 不算数 / 最小充分证据」映射；失败案例 4 条全部为本仓库真实发生的诚实事件（F1 捏造徽章 40+ chains、F2 捏造探针 PASS 留痕、F3 CHANGELOG 计数超前、F4 多表面计数漂移），均注明修正提交（`9e0f558`、`132ae48`）。同步：SB5 计数 10→11；SKILL.md References（中英）与 README 目录树、语言矩阵登记新文件。
差异化定位声明（对生态对标的回应）**：同类最大对标 obra/superpowers 已验证「证据先于声称」方向（其 verification-before-completion 为高安装量单 skill），但其无多智能体治理层——本 skill 独有的指挥官模式、23 字段任务包、身份互斥、DRI 闭环与跨 AI 转交协议为差异化护城河，后续 README 定位段将突出该层（待下一轮文案修订）。
静态自检运行器 `scripts/selfcheck.py`**：11 项机器可判的一致性 / 防漂移静态检查——版本一致性、self-test 编号 1..67 连续、结构完整、身份/references 计数、全库围栏配对、交叉引用存在性、24 字段防泄漏、门禁字段多表面同步、安装平台参数。实测 11/11 且通过负向验证（改坏 VERSION 即 FAIL）；`python scripts/selfcheck.py [--out <file>] [--label <tag>]` 可输出留痕报告。回应「67 条不可自动化」硬伤：不替代行为自测，只做机器可判的仓库完整性检查。
第三方盲测方法 `docs/field-tests/third-party-blind-test.md`**：面向「自产自销、无第三方复现」批评的第三证据维度。对照 / 实验组提示词不含任何 skill 内部术语，测公开 README 能否独立把纪律教给陌生 AI；已登记为「Design·尚未运行」。
README 宿主优先劝退段**：宿主已自带同等规划/审查/验收时优先用宿主原生流程，避免重复叠纪律（When To Use 段尾）；目录树补 `scripts/selfcheck.py`。
自测驱动/归档器 `scripts/selftest-runner.py`**：把 67 条行为自测变成可操作机制——`list` 导出逐条提示词供宿主驱动；`schema` 生成判定表（判定列交由人工填写，工具不自行批语行为）；`archive` 对已填表做统计 + 内容指纹生成可复现报表。待判定项计作「未运行」而非「通过」。
第三方盲测首次执行** `docs/field-tests/blind-test/2026-09-08/report.md`：用两个无 skill 上下文的独立子代理分别跑对照组/实验组。Delta 集中在「先澄清目标/范围/验收」与「先比较方案再动手」——一份不含 skill 内部术语的五行纪律摘要即可让陌生 AI 新增这两类行为，判定 README 文档可复制性成立；「证据/诚实」两项两组皆强，判定为宿主原生自带，印证 README 宿主优先段。已如实标注局限（proxy、宿主浏览器工具放大证据项、单样本）。
对使用侧的回应（采纳外部评审、拒绝其一条建议）**：① README 门面加「名称简注」——名里的“推理风格”指流程纪律、不是提升推理上限，防被名误导；② Usage 强调仅按需显式调用、非常驻，并给 `docs/minimal-discipline.md` 速查卡入口（三条核心常驻）；③ field-tests 索引新增「A/B 验收指南」——读者自测只看交付缺陷数与 token 两个数。**明确拒绝**外部「放宽『从零新建产物默认中档』以省 token」的建议——那是三轮探针实测换来的反绕过边界，放宽会重演轻通道回归；速查卡中写明保留该边界。
轻通道边界口径统一（一致性修复，无行为反转）**：修复「从零新建产物」三种口径并存的矛盾——SKILL.md 排除项引导语「命中任一即不适用」与例外句互斥、openai.yaml 绝对禁止（never take the light tier）、agent-modes 自选第 1 步缺排除项。按 INTERNAL-HISTORY 记载的实测权威口径统一六表面（SKILL / workflow / agent-modes / openai.yaml / README / 速查卡）：新建产物默认中档、除非指令已完整指定类型·位置·形态；self-test Test 56 补该例外正例。
盲测执行状态登记与计数修正**：field-tests 索引与方法文档原标「尚未运行」，实已首测落盘——索引改登记 2026-09-08 已执行并链接报告；方法文档归档路径 `<date>-<ai-alias>/` 放宽为 `<date>[-<ai-alias>]/`；修正「四行纪律摘要」计数（实为五行）。
README 门面与目录树补漏**：修复斜体标记破损（未闭合与悬挂 `_`），目录树补 `scripts/selftest-runner.py`。
速查卡数字伦理**：minimal-discipline 的「常驻不到 50 token」「约八成流程收益」为无出处具体数，改为量级估算并显式标注估算；field-tests A/B 指南补全「两个数怎么判」的决策规则。
对 AI 丁 二轮实测反馈的回应（采纳四项、如实驳回一项、暂缓一项）**：P0-2「版本重号」经核对**本地已不存在**——CHANGELOG 仅有单个 `1.1.0 - 2026-09-08`，2026-08-08 旧公开发布线已于 9398c4f 归档进 INTERNAL-HISTORY，AI 丁 测的是未推送的陈旧 GitHub 副本，因此不加 `+legacy`、不改号；P1-1「自动化同步表面」落地为 selfcheck 新增 **SB11**（轻通道排除边界七表面一致：SKILL/workflow/agent-modes/README/openai.yaml/速查卡/self-test），并补 `.github/workflows/selfcheck.yml` 在 push/PR 时跑 selfcheck + selftest list 解析核验；P0-1「Test 1 无效」属实——本 skill 不自声明语言匹配、宿主兜底，Test 1 改为测「跨语言时签名术语 UNVERIFIED/P0/P1/P2 与引用字节级保真」（宿主不保证、本 skill 证据纪律覆盖，且不与 Test 16 加载证明重复）；P0-3「67 条状态标注」采纳其正交三态分类（FIELD-TESTED/EXAMPLE/RULE-ONLY）但**不臆造状态**——selftest-runner schema 判定表新增 Status 列（默认 ?，由人工填）；P1-2「探针 JSON 化」因外部未提供 JSON 且改动量大、列为下一步本轮不做；AI 丁「别急着补全 67 条、优先补高频高危裸规则」的取舍原则采纳。
语言策略声明（分层双语·中文主导）**：经全库实测确认本 skill 并非统一双语而是分层混合（入口 SKILL.md 中文为主、references 全英文、身份双语、示例中文），且无任何语言承诺。采纳「声明分层、不改内容」策略：README 新增 **Language Policy / 语言策略** 小节——明确「中文为主导语意、英文承载深度规则」、逐层语言矩阵（SKILL.md 中文+英文术语 / README 双语 / workflow·lessons·self-test·identity-library·commander-roles·platform-installation 英文 / agent-modes·closure-rules 英文带中文 / examples 中文 / identities 双语 / docs 中文 / project-policy-template 英文）、以及「层间切换是设计特性而非缺陷」的读者预期。selfcheck 新增 **SB14**（语言策略锚点存在 + 矩阵列出的层文件存在），全量升到 **14/14**。
agentskills.io 规范合规与工具化探针（针对生态对标结论）**：① `uvx skilllint@latest check` 修复 FM009（SKILL.md description 含 ASCII 冒号未加引号致 YAML 解析不稳）后全绿 exit 0——name/description/body 约束、token 量级、行数全部符合规范；② selfcheck 新增 **SB12**（agentskills.io spec 机器可判子集：name 格式与目录名一致 / description ≤1024 / body ≤500 行）与 **SB13**（探针场景注册表与 field-test-2 三轮回合一致、JSON 可解析），全量升到 **13/13**；CI 增加 `uvx skilllint` 步骤与 `probe-runner list` 解析校验；③ 三轮对抗探针脚本化：`probes/probe-scenarios.json`（统一提示词 + 每轮攻击目标/通过条件/失败模式/修复回灌）+ `probes/probe-runner.py`（list/archive/report，判分由人读宿主输出决定、留痕 append-only、绝不自动判 PASS）——探针从手工记录变成可重跑的回归仪器；④ README badges 补 `agentskills.io compliant`（由 skilllint exit 0 验证）；**`platforms` badge 回退为「13 supported」**——上一条目曾写作「40+ chains」，经全项目复查确认该数字无仓库内依据（install.ps1 实含 13 个安装键 / 12 个独立路径，其中 `antigravity` 与 `agents` 指向同一 `~/.agents/skills/` 属文档化别名），「40+」系将对生态 spec 采纳情况的推断误挂到本 skill 自身分发量上，属不实声明，予以回退并记录更正。Verification 与目录树同步补 probes/。
闭环规则补全（AI 丁 推荐「优先补高频高危裸规则」）**：`references/multi-agent-closure-rules.md` 补 **Findings Ledger / 发现账本**（fix-loop 触顶时的升档产物）、强化 **Identity Mutual Exclusion**（身份切换须用户显式授权）、明确 fix-loop cap 继承规则；`references/agent-modes.md` 补 **adjudication chain 裁决链**（用户指名冲突时的优先级）与 **form-change ledger 形态变更账本**（同一任务内形态变更记台账 + 冷却期）。皆为规则补全、无现有规则反转。
自测扩容与去重：67 → 77 条**：① Test 9 原复合题拆成 5 个单点测试（Test 9/10/11/12/13），拆分后每个测试对应单点行为；② 去重三对重复提示词：Test 8/9（同 prompt 验收检查 → 8 验收-focused / 9 bug-sweep-focused）、Test 11/12（同 prompt「俄罗斯方块」→ 11 评估优化-focused / 12 澄清确认-focused）、Test 33/35（同 prompt「任务包」→ 33 信任层级-focused / 35 接收方身份-focused）；③ Test 15/16/18/28 保留为四个独立单点测试（权限请求句 / 渐进加载证明 / 按需读引用 / 三形态陈述），各自 prompt 与期望不重叠、无需合并；④ 新增 6 条高危缺口测试（G1-G6 → Tests 72-77）：身份互斥、证据落盘、Fan-In 合并、声明点禁无身份、用户纠正停摆、切回重声明。
工具链同步**：① `selfcheck.py` 计数更 77 条、新增 **SB15**（自测提示词唯一性，去重后验证）→ 全量 15/15；② `selftest-runner.py` 判定表 schema 补 **Fixture 预置真相** 列（注明测试所需预置准备，跨宿主可比）；③ `docs/field-tests/README.md` 补 **天花板效应警告**（基于二次盲测实证）：0 vs 0 缺陷数不代表纪律无效，仅因任务无区分空间，要测差须任务有真实误解陷阱。
- 纪律核验：改进后全量 selfcheck 15/15 通过、skilllint 0 警告；无规则行为反转、不升版、VERSION 保持 1.1.0。

## 1.1.0 - 2026-09-08

**对外版本线重定基线**（Public version-line rebase）。GitHub 公开发布线从 1.0.0 起算，本版本为 **1.1.0**；内部迭代曾达 3.2.2（2.4.0 → 3.2.2），全部成果合并入此发布，对外不跳号。

This release consolidates all internal iterations into the public line:
重命名**：`gpt-5-6-sol-multi-agent-style` / "GPT-5.6 Sol" → `gpt-series-reasoning-style` / **GPT系列推理风格**；全部示例去品牌化（用 AI 泛称）。
模式自选（v3.1.0-3.2.1）**：AI 按任务事实自选单 Agent 主干 / 子 Agent 增强 / 指挥官扩展，门禁声明一行理由；用户指名优先；命中并行信号（"同时/一起做"）时成本判断必须显式写出。
门禁字段四处表面同步**：形态选择字段已同步至 SKILL.md 字段清单、workflow 门禁模板、openai.yaml、README 示例；轻通道显式排除项（新建产物 / 多交付物 / 并行信号）。
指挥官模式三重构（v3.0.0）**：每个 AI 成员一个身份 md（docs/agents/），派发前指挥官先读、接收方先读自己的身份文件；从不询问底层模型；指挥权接管（用户授权）；变更管理；既有产物冲突先报告。
证据纪律**：存在性检查先于内容核验；DOM 状态变化 ≠ 渲染证据（视觉验收读 computed style）；证据必须有落盘地址。
修复**：任务包字段权威计数 24→23；Test 48 与模型解耦冲突；轻通道被枚举绕过；规则自相矛盾三处。
自测 61 → 67**；README 新增 Maintainer Notes（6 个同步表面）；两份外部评审消化，两项过度优化提案明确拒绝并记录理由。
完整去 Sol（Full de-Sol pass）**：参考文件改名 `sol-reasoning-workflow/lessons/examples.md` → `series-reasoning-*.md`（全部交叉引用更新）；openai.yaml、lessons、社交预览 SVG 的 "Sol-style"/"Sol 风格" 全部中性化；**社交预览 PNG 以新 ASCII 标题重新生成**。仅命名一致性，无行为变化。
发布前一致性补漏（全文通读）**：修复上一轮重命名与字段计数的残留——SKILL.md description 及 workflow/examples/lessons 三个 H1 的 “Sol” 字样；agent-modes.md 三处任务包计数仍写 24（对齐权威值 23）；workflow【实现前确认】模板错位混入半句任务包字段，并补回缺失的“需要你确认”字段与代码块闭合（原未闭合致 Rules 列表被吞入代码块）；commander/executor/planner/reviewer/requirements-analyst/acceptance-auditor 六个身份文件由 6 段补齐为契约声明的 8 段（新增 Mission、Handoff，单数 Responsibility 改为 Responsibilities，内容均从各角色原有语义提炼、不新增规则）；lessons/examples 两处“待复验”统一为 UNVERIFIED。仅一致性修复，无规则行为变化；全库检索 Sol/24-field/待复验 0 命中、全部 .md 围栏成对、23 个身份文件均含 Mission+Handoff，内容改动限 11 个文件（不含本 CHANGELOG）。
二轮全项目复审（交叉引用 / 计数 / 安装文档）**：修正 lessons 对已改名文件的简写引用（`workflow.md` → `series-reasoning-workflow.md`）；统一 platform-installation 的调用示例措辞；README 目录树补列维护脚本 `generate-banner.py`。复审同步核可：任务包权威清单实含 23 字段、自测 67 条、22 个身份文件与 README 权威目录一一对应、门禁十字段在 SKILL.md / workflow / openai.yaml 三表面一致、全部 .md 围栏成对。
三轮全项目复审（任务包口径 / 索引 / 角色表 / 去重）**：① workflow 子 Agent 派发原列「10 项 complete package」，与 agent-modes 六字段迷你包及 self-test Test 58 冲突，改为对齐六字段，并指明需要更完整边界时用本文件内部派发包、跨模型交接用 23 字段完整包；② workflow 指挥官角色选择处连续两条近乎逐字重复（named identity + selection rationale），合并为一条；③ SKILL.md References 清单漏列实存且被 workflow 引用的 `project-policy-template.md`（references 实有 10 份只列 9 份），补齐；④ commander-roles 核心角色表「有副无正」（有 Deputy Planner 无 Planner、有 Code Reviewer 无通用 Reviewer），补 Planner、Reviewer 两行，按规模最小角色集表保持不动。仅一致性修复，无规则行为变化、不升版。经评估刻意不改以避免过度优化：lessons 的 10 项定性委派清单（不声明字段数、非权威模板）、接收方降级简化包 5 字段（外部降级通道，与子 Agent 六字段用途不同）、步骤编号 1.5/2.5（避免全量重编号）、Mode 1/2/3 别名（三名称保留的设计决策）、deputy-commander 第 9 段指挥权接管（角色专属段）、安装表手动 VS Code 行（脚本 13 参数，文档另列手动路径）。
四轮交叉评审消化（外部复审逐条回到磁盘核对）**：修复 `generate-banner.py` 卡片注释 four→three（实际为三卡，逻辑按 len(chips) 动态计算、仅注释残留）；补全 platform-installation 的包结构树（原仅列 SKILL/VERSION/CHANGELOG/references 四项、与真实顶层不符），顶层树与 README 目录结构对齐并指向 README 完整树以避免双源维护。外部所提「徽章 13 与安装表对不上」经核对**不改**：12 = README 脚本表显示行数（agents 与 antigravity 同义合并为一行）、13 = 脚本可接受参数名（徽章口径，ps1 13 键 / sh 13 个参数名 12 个分支）、14 = platform-installation 完整产品行（额外拆出 Antigravity 并含手动安装的 VS Code Copilot），三口径自洽，且 README 已有「antigravity 同义」标注与「VS Code Copilot 手动安装」脚注。仅注释/文档修复，无行为变化、不升版。
社交预览 SVG 文字转曲（跨平台自包含）**：`social-preview.svg` 将全部文字由 `<text>` 转为矢量路径（14 组 path、0 个 text 元素），不再依赖查看者本机 CJK 字体，任何平台打开渲染一致；补 XML 声明与 `title`/`desc` 无障碍标注、移除冗余标题阴影滤镜。视觉设计、三卡文案、版本（v1.1.0 · MIT）与 `social-preview.png` 保持一致；PNG 仍是 GitHub 实际展示图、设计未变故无需重渲染。仅源文件技术优化，无内容/行为变化。
版本号全项目对齐审计**：修正「发布前内部历史」引导语中英两处「当前公开版本 1.6.0」→`1.1.0`（对外重定基线时漏改的当前版本指针）；核可 VERSION、SKILL.md、README 徽章与目录树、横幅脚本与 SVG desc、self-test/examples 加载证明、git tag 与 Release 全部为 1.1.0；历史条目（含 2026-08-08 旧公开线 1.1.0、内部 0.1.x–3.x 及各「同步到 x.y.z」记录）按史实原样保留，不视为不一致。
改名可检索性 / 搜索消歧**：README 顶部新增中英「Formerly / 曾用名」映射，显式关联旧名 `gpt-5-6-sol-reasoning-style` 与新名（实测旧网页 URL 301 跳转、旧 API 跳转到仓库 ID 1326814334），便于搜索引擎与联网 AI 把旧名归并到本仓库；旧名在 README 仅此一处，专用于被检索后归并。
实测证据归档与定位强化**：新增 `docs/field-tests/`（双语索引 + 两份脱敏报告：①指挥官形态端到端实测 16/16（触发项全过口径，2 个陷阱未触发不计分、如实记未测出），含"指挥官抓到执行者未发现的 P0——选择器从未匹配、效果从未渲染"案例；②三轮对抗探针系列——模板缺字段 / 轻通道旁路 / 规则自相矛盾各一层，均回灌修复）；README 定位句由「行为覆盖型 Agent Skill」强化为「交付纪律行为层」（把"Agent 说做完了"变成"Agent 证明做完了"），验证小节同步链接实测报告。仅文档与定位措辞，无规则行为变化、不升版。
- 验证：版本引用全同步、品牌零残留、无私有项目名泄漏、自测 67 条。

完整内部迭代历史（`2.4.0`→`3.2.2` 长线，以及更早 `0.1.x`–`3.x` 与旧公开线）已归档，见 [`INTERNAL-HISTORY.md`](INTERNAL-HISTORY.md)；对外版本从当前 `1.1.0` 起算、按 `1.1.x` 递增。
